# Dependencies for the Career Agent NLP microservice (nlp_service/main.py).
#
# cmd/agent reaches this service at the hardcoded http://localhost:8000/process
# (pkg/mcp/client.go) whenever profile.yaml sets use_master_cover_letter: false,
# with no fallback -- so with that toggle off and this service down, every
# tailored-document generation in the run fails. See the README's launch
# section.
#
# Lower bounds are the versions this service has actually been run against,
# read from the working venv rather than chosen; upper bounds are the next
# major, since fastapi/pydantic both break APIs across majors.

fastapi>=0.141.1,<1.0
pydantic>=2.13.4,<3.0
requests>=2.34.2,<3.0

# Not imported by main.py -- it is the ASGI server that runs it
# (`uvicorn main:app --port 8000`).
uvicorn>=0.52.0,<1.0
