# improvements_paywall.md — Archived Groom-Pass / Status History

**2026-08-01 groom-pass note (post-#489 run, thirty-ninth session):** all three rows re-verified against current code and live state, no score changes — **#424 stays 2.0**, **#17 stays 1.75**, **#14 stays 0.43 and `⚠️ below floor`**. Repository-wide grep for `2captcha|capsolver|anticaptcha|deathbycaptcha` across every `.go` file and `go.mod` still returns 0 hits; `pkg/submitter/` still has 0 references to `ANTHROPIC_API_KEY`/`GEMINI_API_KEY`; `lspci` still reports only the integrated `Radeon Vega Series` GPU. This session's work item (bug #489, `pkg/security/filter.go`'s prompt-injection quarantine false-positive fix) touched only `pkg/security` — none of CAPTCHA solving, cloud DOM-offload routing, or GPU availability, the three premises these rows depend on.

Superseded dated status paragraphs moved out of `improvements_paywall.md` during the 2026-08-01 backlog-size restructure (see `documentation/task_journals/2026-08-01_optimize-backlog-access.md`). The file itself now keeps only the single current status paragraph plus the still-actionable standing recommendation on #14. This file is the full historical record, kept for audit, not read on a normal work session.

**Prior — 2026-07-31 full `/groom_backlogs` audit (twenty-ninth session):** all three rows re-verified against current code and live state, no score changes — **#424 stays 2.0**, **#17 stays 1.75**, **#14 stays 0.43 and `⚠️ below floor`**. `grep -rn "gemini\|claude" pkg/parser/*.go` (excluding tests) still returns nothing; repository-wide grep for `2captcha|capsolver|anticaptcha|deathbycaptcha` across `*.go`/`go.mod`/`go.sum` still 0 hits; `lspci` still reports only the integrated Radeon Vega Series GPU. This session's work was a backlog-only audit (no code shipped) that filed `bugs.md` #476 (`pkg/storage`) and corrected a stale operational note in `bugs.md` — neither touches CAPTCHA solving, cloud DOM-offload routing, or GPU availability, the three premises these rows depend on.

**Prior — 2026-07-31 groom-pass note (post-#450 run):** all three rows re-verified against current code and live state, no score changes — **#424 stays 2.0**, **#17 stays 1.75**, **#14 stays 0.43 and `⚠️ below floor`**. `grep -rn "gemini\|claude" pkg/parser/*.go` (excluding tests) still returns nothing, so DOM parsing still has no cloud routing; repository-wide grep for `2captcha|capsolver` still 0 hits; `lspci` still reports only the integrated Radeon Vega Series GPU. This session's work item (`improvements.md` #450, splitting the shared SQLite DSN by reader/writer role) touched `pkg/storage` and `cmd/dashboard` only — none of CAPTCHA solving, cloud DOM-offload routing, or GPU availability.

**Prior — 2026-07-31 groom-pass note (post-#470 run):** all three rows re-verified against current code and live state, no score changes — **#424 stays 2.0**, **#17 stays 1.75**, **#14 stays 0.43 and `⚠️ below floor`**. This session's work item (`improvements.md` #470, `cmd/requeue -status` pre-flight validation) touched only `pkg/storage/manager.go` and `cmd/requeue/main.go` — none of CAPTCHA solving, cloud DOM-offload routing, or GPU availability. Repository-wide grep for `2captcha|capsolver|anticaptcha|deathbycaptcha` still 0 hits; `pkg/submitter/*.go` still 0 references to `ANTHROPIC_API_KEY`/`GEMINI_API_KEY`; `lspci` still reports only the integrated Radeon Vega Series GPU.

**Prior — 2026-07-31 groom-pass note (post-#469 run):** all three rows re-verified against current code, no score changes — **#424 stays 2.0**, **#17 stays 1.75**, **#14 stays 0.43 and `⚠️ below floor`**. This session's work item (improvements.md #469, per-domain circuit breakers for `cmd/agent`'s fetch/pre-flight retries) touched `cmd/agent` and `pkg/storage` only — none of CAPTCHA solving, cloud DOM-offload routing, or GPU availability, the three premises these rows depend on.

**2026-07-31 groom-pass note (post-#466 run):** all three rows re-verified against current code and live state, no score changes — **#424 stays 2.0**, **#17 stays 1.75**, **#14 stays 0.43 and `⚠️ below floor`**. A repository-wide grep for `2captcha|capsolver|anticaptcha|deathbycaptcha` across every `.go` file and `go.mod` still returns 0 hits; `pkg/submitter/` still has 0 references to `ANTHROPIC_API_KEY`/`GEMINI_API_KEY`; `lspci` still reports only the integrated `Radeon Vega Series` GPU. This session's work item (bug #466) touched only `pkg/storage`, `cmd/agent/pipeline.go`, and `cmd/requeue` — none of the three premises here.

**Groom history, consolidated 2026-07-29.** Eleven consecutive groom passes between 2026-07-27 and 2026-07-28 recorded the same two findings in near-identical prose; they are collapsed here, losing no distinct fact. Across every one of those passes, **#17 held at 1.75 = 7×1.0÷4** and **#14 held at 0.43 = 3×1.0÷7, below the floor**. The re-verification was the same each time and is worth stating once properly:

- **#17** — CAPTCHA detection and `BLOCKED_CAPTCHA` routing exist and work; what does not exist anywhere in the repository or the live environment is a solver client, a provider dependency, a solve-and-continue path, or a configured 2captcha/Capsolver key. Successive passes confirmed that nothing in the intervening work (#120, #121, #122, #123, #129) changed any of that. Still gated on an explicit paid provider choice and key.
- **#14** — the host still exposes only an integrated Radeon Vega GPU, so a useful fine-tuning experiment needs paid cloud compute; and the database still holds no *human preference labels* (the passes measured one tracked outcome and 74 model-generated fit scores, which are model output, not labels), with no training or evaluation pipeline present. Deferred until a preference-labeled dataset exists.

**2026-07-29 groom-pass note (post-#423):** this file gained **#424 (Cloud-Offloading for DOM Parsing)**, moved unchanged from `improvements.md` at its existing 2.0 = 8×1.0÷4. It was mis-filed in the free backlog: its premise is routing heavy DOM mapping to cloud models, which requires a paid API key, so it was reachable by `/work_next_item`'s autonomous selection when it should never have been. #424 is now the highest-scoring row here, but like everything in this file it is only worked when the user explicitly names it.

**2026-07-29 groom-pass note (evening, post-#435 run):** all three rows re-verified against live state, no score changes. **#424 remains 2.0**, **#17 remains 1.75**, **#14 remains 0.43 and below floor**. Evidence this pass rather than inheritance from the notes above: a repository-wide grep for `2captcha|capsolver|anticaptcha|deathbycaptcha` across all `.go` files and `go.mod` returns **nothing**, and no solver key of any of those names is present in the environment — so #17's premise is unchanged. `lspci` still reports the integrated `Radeon Vega Series` as the only display adapter, so #14's is too. #424 needs no re-verification beyond its own premise, which is definitionally paid.

**2026-07-30 groom-pass note (post-#437 run):** all three rows re-verified against current code and live state, no score changes — **#424 stays 2.0**, **#17 stays 1.75**, **#14 stays 0.43 and `⚠️ below floor`**. Evidence gathered this pass rather than inherited: a repository-wide grep for `2captcha|capsolver|anticaptcha|deathbycaptcha` across every `.go` file and `go.mod` returns **0 hits**, so #17 is still entirely unbuilt; `pkg/submitter/` contains **0** references to `ANTHROPIC_API_KEY` or `GEMINI_API_KEY`, so #424's premise (routing DOM mapping to a paid cloud model) is still unimplemented — independently corroborated this session by bug #444's finding that no DOM-parsing path calls a paid endpoint directly; and `lspci` still reports the integrated `Radeon Vega Series` as the only display adapter, so #14 still has no local GPU to make it free. **#14 stays open and below floor**: it needs explicit user confirmation before anyone works it — the recommendation remains to leave it parked rather than close it, since a future GPU changes its economics entirely rather than its merit.

**Prior 2026-07-30 groom-pass note (post-#441 run):** all three rows re-verified against current code, no score changes — **#424 stays 2.0**, **#17 stays 1.75**, **#14 stays 0.43 and below floor**. Evidence gathered this pass:

- **#17** — a repository-wide case-insensitive grep for `2captcha|capsolver|anticaptcha|deathbycaptcha` across `*.go`, `go.mod` and `go.sum` returns nothing. The only CAPTCHA-solving reference in the codebase is a comment at `pkg/submitter/browser.go:241` that names this very item as the (paid, user-gated) owner of the solve step, so detection is intact and only solving is missing. Premise unchanged.
- **#424** — re-checked the half of its premise that can be checked without a key: no DOM-parsing path calls a hosted endpoint directly. `ExtractFormMapping` (`pkg/mcp/client.go:635`) and `ExtractFormMappingVision` (`:700`) both route through the same `c.generate` as every other call, with the provider chosen by `LLM_PROVIDER` at `pkg/mcp/provider.go:40`. Worth flagging for whoever authorizes this: `pkg/submitter/vision.go` **logs** "Transmitting screenshot to Gemini-1.5-Pro" on that path today while a local model actually answers, which is now bug #444 — anyone scoping #424 should not read those log lines as evidence that cloud routing already exists. Effort stays 4, score stays 2.0.
- **#14** — not re-probed this pass; its two blockers (paid GPU compute, and a labeled preference dataset that is still not being collected anywhere in the schema) were verified in detail on 2026-07-29 and nothing in the #441 run touched either. The standing recommendation below is unchanged and is now ten passes old.

**2026-07-30 groom-pass note (eighth session, post-#454 run):** no score or rank change — **#424 stays 2.0**, **#17 stays 1.75**, **#14 stays 0.43 and `⚠️ below floor`**. This session's work item (`improvements.md` #454) was a documentation-only process fix with no bearing on any of these three premises, so this pass re-confirms rather than re-derives: nothing in this session's diff touched CAPTCHA solving, cloud DOM-offload routing, or GPU availability.

**Prior — 2026-07-30 groom-pass note (model-accuracy pass):** no score or rank change — **#424 stays 2.0**, **#17 stays 1.75**, **#14 stays 0.43 and `⚠️ below floor`**. All three premises are definitionally paid-gated and were re-confirmed unchanged; the substantive edit this pass was to the Claude model column, under improvement #455. **#14's column named `claude-opus-4-6-thinking`, which is not a model ID at all** — no `-thinking` suffix exists in the Anthropic lineup, because extended thinking is a request parameter and not part of a name. It now reads `claude-opus-5`. #424 and #17 moved `claude-sonnet-4-6` → `claude-sonnet-5`; that one was a real, still-served model, just a generation behind.

The note below reassessing #17's model on 2026-07-26 is left as the dated record it is, and is the origin of the `claude-sonnet-4-6` value — accurate when written.

**Prior — 2026-07-29 groom-pass note (late evening, post-#439 run):** all three rows re-verified live, no score changes — **#424 stays 2.0**, **#17 stays 1.75**, **#14 stays 0.43 and below floor**. Evidence gathered this pass rather than inherited:

- **#17** — a repository-wide grep for `2captcha|capsolver|anticaptcha|deathbycaptcha` across `*.go`, `go.mod` and `.env.example` returns nothing, and no solver key of any of those names is in the live environment. `isCaptchaBlocked` and `BLOCKED_CAPTCHA` routing still exist, so detection is intact and only the solve step is missing. Premise unchanged.
- **#14** — `lspci` still reports the integrated `Picasso/Raven 2 [Radeon Vega Series]` as the only VGA controller. Separately checked the *other* half of its premise this pass, which earlier passes asserted from prose: the schema has ten tables and a grep for any `user_label`/`user_rating`/`user_feedback`/`user_preference`-shaped column across `pkg/` and `cmd/` returns nothing. There is still no place a human preference label could be stored, let alone a labeled dataset. Both blockers stand.
- **#424** — definitionally paid, so its premise needs no re-verification. Bug #439's work is relevant to it, though, and worth recording for whoever authorizes it: that fix restored and re-tested the `pkg/mcp` provider abstraction (`LLM_PROVIDER` → `ollama`/`claude`/`gemini`) which #424 would build on, and it confirmed a per-call-type routing precedent already exists in `genRequest.fast`. That precedent is model selection *within* one provider, not routing *across* providers, so **Effort stays 4**: the plumbing helps, but a paid-key item cannot be verified without the key, and unverifiable work is not cheap work. Score unchanged at 2.0.

**Recommendation to the user, unchanged and now nine passes old for #14:** #14 has scored below the ROI floor on every pass since it was filed, for a reason that will not change by waiting — it needs paid GPU compute *and* a labeled dataset that does not exist and is not being collected. It stays open per the rule that below-floor items are never closed unilaterally, but re-confirming it every pass has produced no new information for over a week. **Consider closing it, or re-scoping it to the much smaller "start capturing preference labels" precursor that would actually make it viable later.**
