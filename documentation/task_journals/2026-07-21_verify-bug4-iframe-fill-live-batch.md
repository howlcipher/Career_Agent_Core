# Task Journal: Verify bug #4 (iframe form-fill fix) via live cmd/agent batch run

## Summary

- **Task:** bugs.md #4 — `AttemptSubmit` form-fill logic never looked inside iframes. Fix applied 2026-07-20 (`resolveFillTarget` in `pkg/submitter/browser.go`), still unverified live — no case has ever exercised the iframe-fallback path itself. Working live batches since 2026-07-21 toward a genuine `APPLIED` status to close the Usability Gate's live-batch checkbox; that work fanned out into ~20 other real, independently-diagnosed bugs (#5 through #44), all detailed in `bugs.md` directly rather than here.
- **Status:** In progress
- **Started:** 2026-07-21
- **Agent and model:** Claude Code / Sonnet 5 (orchestrator; delegates real fixes per the Working Protocol when well-scoped)

## Pre-Flight Re-Evaluation (last done 2026-07-23)

- **Usability Gate check:** NOT MET. Static checks, Ollama install, `cmd/dashboard`, and `cmd/tracker` crash-safety are all verified. The two remaining gaps are the live-batch `APPLIED` checkbox (this task) and zero open Blocker/Major bugs (currently #4, #8, #10, #14 are Major/Pending, #39 is Minor/Pending — see `bugs.md`).
- **Model choice:** No delegation needed for observation; code fixes so far have been split between direct edits (well-understood, small) and `agy` delegation (larger, clearly-scoped diffs, always verified against `git diff` before trusting).
- **Code re-verified 2026-07-23:** `go build/vet/test ./...` all pass clean. `resolveFillTarget` (#4), `clickApplyIfPresent` (#8), the Vision-fallback-on-fill-failure branch (#10), and the label/placeholder fill tiers (#14) are all present in `pkg/submitter/browser.go` exactly as `bugs.md` describes.

## Progress Log (condensed — full blow-by-blow lives in bugs.md's Detail sections, not duplicated here)

- 2026-07-21 — Long live-triage session. Diagnosed and fixed #5 through #17 in sequence, each surfaced by actually watching a live batch run rather than guessing (iframe-fallback #4 fix from 07-20 never got a positive hit; every real failure this session turned out to be a *different* bug: click-to-reveal forms #8, dead/expired postings #9, FunnelEngine false positives #7/#11, duplicate reprocessing #12, Ollama OOM #13, accessible-label fill fallback #14, placeholder-tier fallback #16, UTC sort-order bug #17). Also shipped several dashboard/docs improvements the same session (improvements #19/#20/#21 groundwork). Five orphaned `go run` child processes were found contaminating one observation window (documented as the Operational Trap in `bugs.md`) — cleaned up, and all fixes are believed correct on their own merits (each was root-caused from direct evidence: standalone diagnostic scripts, direct DB reads, `journalctl`), but full live confirmation of #4/#8/#10/#14 specifically still requires a clean run reaching a fresh `APPLIED`.
- 2026-07-22 — Further live triage (Claude Code session, ~6 hours): found and fixed a real environment regression (agent running on the bare Bazzite host again instead of the `career-agent` distrobox, causing silently-blank page renders), then a chain of real interaction bugs once the environment was correct again: cookie-consent backdrop blocking clicks (#34), SmartRecruiters' "I'm interested" wording plus a post-click CAPTCHA reveal (#35), Jobvite's data-consent `<select>` gate (#36), a too-tight fill-action timeout under CPU contention (#37), and a 0%-success source (`breezy.hr`) plus Workday queue monopolization (#38). Also excluded/fixed several junk-URL classes (applytojob.com and recruitee.com board indexes, #41; BambooHR marketing/login pages, #42; a Playwright strict-mode violation on ambiguous labels, #43) and BambooHR's growing subdomain denylist replaced with a positive path-shape check (#44). No fresh `APPLIED` was produced despite all of this — the last real fill attempt hit a distinct, still-open bug (#39, Vision-fallback returns an empty selector).
- 2026-07-23 — Groom pass (this session's parent task) confirmed via a fresh log read that a live batch is still running unattended, still reaching real jobs, but still only hitting the already-known SmartRecruiters DataDome rate-flag (`BLOCKED_CAPTCHA`, expected per #23's operational note) — no new `APPLIED` yet today. No new code changed in this task itself; scope was backlog documentation accuracy only.

## Next Step

**Live batch running right now, independent of any chat session:** PID 2970877 (`/tmp/career_agent_bin_live11`, has all of #45-#53's fixes), started 2026-07-24 ~10:50, running inside the `career-agent` distrobox. It is a real OS process — clearing/starting a new chat session does NOT stop it. `applications.db`'s `job_funnel` `APPLIED` count was 82 as of 2026-07-24 ~10:50 (restart baseline). On resume: `distrobox enter career-agent -- ps -p 2970877` to confirm it's still alive, `tail -f career_agent.log`, and re-arm a Monitor (persistent, checking `SELECT COUNT(*) FROM job_funnel WHERE status='APPLIED'` every ~2 min, since a chat session's own Monitor doesn't survive a clear) — don't kill/restart it just to attach a fresh monitor, that only loses progress.

**Bug #53 (major, same night): isSubmissionConfirmed was only reachable from the Lever/Greenhouse/LinkedIn dispatch path.** Every other ATS (SmartRecruiters, Ashby, Homerun, Pinpoint, Jobvite, BambooHR, applytojob.com, recruitee.com, etc. — the cached-mapping fast path and both AttemptVisionSubmit call sites) returned success straight from `handleDynamic`'s bare error value, with zero confirmation evidence — the exact unverified pattern bug #51 fixed for only three of many paths. Also fixed the URL-baseline defect that started this investigation: `isSubmissionConfirmed` compared against the *original job-posting URL* instead of the URL right before each specific submit click, which bug #47's click-to-reveal step made trivially "changed" regardless of whether the click did anything. Live-probed a real, untouched Lever posting (read-only, no submission) and confirmed native HTML5 `required` fields with no `formnovalidate` override — a blank required field silently blocks the submit client-side with zero navigation and zero error text, exactly the gap this baseline bug masked. Full fix: `confirmOrError` shared helper, `urlBeforeSubmitClick` threaded through every dispatch branch, `AttemptVisionSubmit` made self-contained. 4 new tests. Not yet verified live for a non-Lever/Greenhouse/LinkedIn platform — watch the next Ashby/Homerun/etc. success closely, since those are the paths that just went from "unconditional trust" to "real check" for the first time ever.

Every `[Auto-Submit] Submission confirmed for %s (%s) at %s` log line names which of `isSubmissionConfirmed`'s three evidence tiers fired AND the destination URL — check these first if false-positive concerns come up again, before spending time on Gmail (which turned out to be unreliable tonight: the user may be deleting confirmation emails, and 0/20 of tonight's real Lever applications had any traceable email).

**Re-verification run in progress (separate from the above — supersedes it until complete):** user asked to requeue all 82 then-`APPLIED` jobs (39 Lever, 32 Greenhouse, 7 Workday, 2 SmartRecruiters, 2 Pinpoint) and re-test them now that bug #53's fix is live, explicitly accepting the risk of a real duplicate submission for any that were genuine successes. Extended `TARGET_JOB_URL` to accept a comma-separated list (`cmd/agent/main.go`, `parseTargetJobURLs`, tested) and `cmd/requeue`'s `-status` flag to accept `APPLIED` (`countForStatus`, tested) — both committed/pushed before executing. Ran `cmd/requeue -source lever,greenhouse,workday,smartrecruiters,pinpoint -status APPLIED -confirm -clear-dedup`: exactly 82 rows reset `APPLIED` → `DISCOVERED`, confirmed via `SELECT COUNT(*) WHERE status='APPLIED'` = 0 immediately after. Per the user's explicit choice, this is a **dedicated, isolated run** — killed the main batch (PID 2970877) and launched `/tmp/career_agent_bin_verify82` with `TARGET_JOB_URL` set to exactly those 82 URLs (confirmed via log: "loaded 82 matching job(s)... 82 targeted"), PID 2978918. All normal discovery/applying is paused until this run finishes and exits on its own (`TARGET_JOB_URL` runs are self-terminating, unlike the normal unbounded batch) — **the normal full-backlog batch must be manually restarted afterward**, it does not resume on its own. The 82 original URLs are saved at `applied_urls_verify82.txt` (repo root, untracked) for the monitor's outcome-breakdown queries; safe to delete once this run completes and its results are recorded here.

**Monitor gotcha hit and fixed, twice:** first completion-detection monitor grepped the *entire* `career_agent.log` for "Batch execution complete", which already had 6 historical matches from earlier restarts tonight — fired an instant false-positive "complete" after only 1 of 82 jobs had started. Fixed with a line-count offset — but that broke *again* once side-quest single-job tests (below) started writing their own completion lines to the same shared log file, since the offset check doesn't distinguish which process wrote a given line. Final fix: track completion purely by process liveness (`kill -0 <PID>`), never by grepping the shared log — the log is fine for outcome-breakdown polling (DB-based, PID-agnostic) but not for completion detection when multiple processes share one log file. Monitor `bkkwdpb84` uses the corrected approach.

**First real outcome from the 82-job run:** Reddit (Greenhouse, fit 90) hit the *same* circuit-breaker payload-size failure bug #52 was supposed to fix (102,963 chars) even with `PruneDOMToForm` live — but a probe confirmed this posting genuinely has a `<form>` element (the fix's scoping logic did apply), so this looks like a legitimately huge Greenhouse form (many custom screening questions) rather than a scoping failure. Filed as a known gap, not yet fixed — noted here rather than in bugs.md since it needs more investigation before a root cause is confirmed.

**Side quest: verifying bugs #8/#10/#14 (the Usability Gate's last 3 open Major bugs).** Their fixes are already shipped and confirmed firing live, but none has a *fresh* confirmed success under the new (post-#53) confirmation logic — and the 82-job run won't reach its 2 SmartRecruiters/2 Pinpoint jobs for hours (Lever/Greenhouse-heavy, sequential). Paused the 82-job run (`kill -STOP` 2978918) and ran quick isolated `TARGET_JOB_URL` single-job tests against other already-`DISCOVERED` generic-Learner-Module jobs instead:
- Ashby (`pragmatike`) and a second Ashby (`real`) both hit the raw-HTML captcha-block check (`cmd/agent/main.go`, bug #46's area) before fit-scoring — root-caused and fixed same session, see bug #54 below.
- BambooHR (`cxm.bamboohr.com/jobs/questions?id=169`) got past that check and reached `ExtractFormMapping` (the generic Learner Module path #8/#10/#14 need) after a genuinely slow ~30 min doc-gen — but after ~1hr15min total with no fill/confirm result yet, user asked whether it was worth the opportunity cost against the paused 82-job run. Killed it (PID `3004280`) and resumed the 82-job run instead — #8/#10/#14 should get confirmed organically once that run reaches its own SmartRecruiters/Pinpoint jobs, without a dedicated side-test.

**Bug #52 recurred twice more after resuming**, both real Greenhouse jobs, both the exact circuit-breaker payload-size failure: Akuity (85,097 chars) and You.com (112,571 chars) — on top of Reddit's original hit. Root-caused and fixed for real this time: `parser.StripPresentationalAttrs` (new), strips styling/state/most-aria attributes that were bloating even a correctly form-scoped payload; confirmed 98,255 → 33,629 chars on the real Reddit form. Given the pattern had now hit 3 real jobs in one run, restarted again (PID 2978918 → 3078164) specifically to protect the ~30 remaining Greenhouse jobs in the 82-job batch — requeued Reddit/Akuity/You.com specifically (not the one legitimate dead-job failure, same "just the diagnosed subset" discipline as bug #49). 78 of 82 matched on this restart (82 − 1 dead-job FAILED_SUBMIT − 2 SKIPPED − 1 orphaned PROCESSING row from the kill, which wasn't reset and may need a manual check later). Found and fixed that orphaned-row gap too, twice (Enveritas, then Akuity again on the next restart) — a job killed mid-`PROCESSING` never gets picked up again since `GetDiscoveredJobs` only pulls `DISCOVERED`; needs a manual `UPDATE ... SET status='DISCOVERED'` after every kill from now on, not just a restart.

**Bug #52 recurred a THIRD time**, again on Reddit specifically (the largest form seen all night) — 54,917 chars, over the two-fix-reduced payload but far below the original 102,963. Probed directly: 35 real input/textarea fields, no dropdown bloat, nothing left to strip without risking real field data — the size is genuinely proportional to field count. Raised `SolveValidationErrors`' limit specifically to 75,000 (`pkg/mcp/client.go`'s `incrementAndLogAPICall` now does a per-call-type lookup instead of one hardcoded constant), still far below the original unfixed sizes. Restarted again (PID 3078164 → 3097288) with Reddit and the orphaned Akuity row both reset to `DISCOVERED`. 3 total restarts purely to chase this one recurring bug across three real Greenhouse jobs (Reddit twice) tonight — each recurrence was a genuinely different root cause (scoping → attribute bloat → real field-count size), not the same fix failing repeatedly.

**Also shipped this session (user asked, unrelated to the above): improvements.md #16 (partial), proactive custom-question answering.** `ExtractFormMapping` now detects and answers custom screening questions during the first fill pass (not just reactively via `SolveValidationErrors` at retry time), grounded in profile facts with the same never-invent-EEO-answers rule, written naturally rather than as canned boilerplate. `FormMapping` gained an `Answers` map; `handleDynamic` fills each one best-effort. 2 new tests, `go build/vet/test ./...` pass, committed and pushed. Does NOT cover Workday's multi-step pre-screening (item 11's state machine still doesn't exist, and Workday is still auth-gated pre-form per #18/#50) — noted in improvements.md's #16 entry. Not yet verified live under real traffic (the BambooHR test above is running an older binary built before this feature shipped, so it won't exercise it) — needs a fresh restart-and-observe cycle once the current tests wrap up.

- 2026-07-24 09:00-09:22 — User asked to verify `APPLIED` jobs actually generate email confirmations, worried about false positives, after the previous session's Instrumentl Lever application had zero traceable Gmail evidence. Searched Gmail broadly (company names, `lever.co` domain, `from:hire.lever.co`, Trash included, no date limit) for all ~20 of tonight's real Lever `APPLIED` jobs — zero results across the board. Also re-searched for "Avive Solutions" (a real Lever rejection bug #51's investigation found and documented just yesterday) and got zero results too, even though general Gmail search was confirmed working via an unrelated control query. User said they may have deleted it, which plausibly explains the whole pattern without any code bug — but couldn't confirm whether they've also been deleting tonight's Lever emails as they arrive, so the 0/20 result stays ambiguous. Independent of Gmail, found and fixed a real observability gap: `isSubmissionConfirmed` (bug #51) had three evidence tiers but returned a bare bool, so there was no way to tell from the log which tier fired for any given `APPLIED`. Added a `submissionConfirmationReason` return value, logged at the call site. No behavior change. 5 existing tests updated to assert the reason too, `go build/vet/test ./...` all pass. Committed and pushed (documented as an addendum to bug #51's Details section, not a new numbered bug, since no defect existed — just missing instrumentation). Restarted the batch (PID 2579802 → 2918507) to pick it up; confirmed sole instance via `ps aux` and log growth, baseline reset to 80 `APPLIED`.

- 2026-07-23 22:10-22:35 — Resumed a watch session on the prior PID (2542429, still alive at 54 min uptime). Log tail caught a genuine new loss: a real Greenhouse posting (fit score 90) generated documents and filled every field, then was aborted outright by `incrementAndLogAPICall`'s 50k-char LLM-cost circuit breaker on the validation-retry call — the payload (104,932 chars) was the *entire* pruned page, not just the form, because `target.HTML()` + `parser.PruneDOM` keeps everything except script/style/svg/iframe/noscript/meta/link. Filed and fixed as **bug #52**: new `parser.PruneDOMToForm` scopes the validation-retry payload down to the first `<form>` element (falls back to the full document if none found), wired into the one call site that needed it. 2 new tests, full suite passes. Committed (code and docs separately) and pushed at the user's go-ahead, per the Working Protocol's default-push rule. User separately asked about ranking the discovery queue by resume-fit similarity in addition to source-priority — investigated (fit_score is lazy/per-job at dequeue time, embedding pre-pass is the cheap bulk option, remote/salary enforcement would stay ScoreJob-only) and filed as **improvements.md #22**, explicitly deprioritized behind the still-open Usability Gate at the user's own request rather than built now. Killed PID 2542429 (confirmed dead via `ps -p`), rebuilt (`/tmp/career_agent_bin_live8`), launched as PID 2579802 (confirmed sole instance via `ps aux` and log growth), requeued the one diagnosed job (`cmd/requeue -pattern '%cobaltio/jobs/8603198002%' -status FAILED_SUBMIT -confirm -clear-dedup`) — but note this requeue landed *after* the new process's queue snapshot was already loaded, so that specific job won't be retried until a future restart or a `TARGET_JOB_URL` targeted run. Baseline reset to 60 `APPLIED` at restart.

Keep watching for #8 (click-to-reveal in the *generic Learner Module* path, distinct from #47's dedicated-handler fix), #10 (Vision fallback after a mapped-fill failure), or #14 (label-fallback fill) to fire and reach a genuine `APPLIED` through their specific path. If any of the three turns out to be similarly unreachable via organic live traffic (as #4 was), fall back to the same targeted-unit-test approach used to close #4 (`TARGET_JOB_URL`, now a permanent `cmd/agent` feature). Once all three are closed, flip the Usability Gate's Status line to `MET` and this task is done — delete this journal in that final commit.

- 2026-07-23 09:46 — User confirmed go-ahead for a fresh live-run session. Found two stray untracked binaries (`career_agent_bin_3`, `career_agent_bin_4`, both dated 2026-07-22) at repo root — stale build artifacts, not running, deleted. Confirmed no `career_agent`/`cmd/agent` process running anywhere (host or container) before starting, and Ollama reachable from inside `career-agent`. New wrinkle: the `career-agent` distrobox's `/usr/bin/go` is a stale 1.18.1 that can't even parse this repo's `go.mod` (`go 1.26.5`) — `/usr/local/go/bin/go` (also 1.26.5) is the one that actually works and must be invoked by full path inside the container; PATH order puts the broken one first. Built a fresh binary with the correct toolchain at `/tmp/career_agent_bin_live` and launched it directly (not `go run`) inside the container, backgrounded with `nohup`/`disown`, PID 2129285, logging to the repo's `career_agent.log`. Confirmed via `ps aux` and log growth that it's the only instance running and actively processing (loaded 2001 backlogged jobs, scoring underway as of 09:47). Now monitoring for a fresh `APPLIED` or a real new failure on the #4/#8/#10/#14 paths.

- 2026-07-23 10:20 — Live batch running clean 30+ min, still 46 `APPLIED` (unchanged), two events so far both resolved to already-known/fixed patterns (bug #8's click-to-reveal firing correctly, a dead-job case same class as #9/#39). User asked whether to try something more targeted than waiting on the batch. Wrote a standalone Playwright probe (`resolveFillTarget`'s exact logic, reimplemented against a real headless browser) and ran it against 6 current SmartRecruiters postings (bug #4's only known repro platform) plus a broader sample (Ashby, Pinpoint, Homerun, Jobvite). **Finding, written up in bugs.md #4:** SmartRecruiters now serves a DataDome CAPTCHA iframe after every "I'm interested" click (correctly caught by bug #35's post-click check before `resolveFillTarget` ever runs), and every other sampled platform renders its form directly on the main page — meaning no platform in current live traffic can organically exercise bug #4's iframe-fallback path anymore. Since live verification is structurally blocked, added 3 direct unit tests instead (`TestResolveFillTarget_*` in `pkg/submitter/browser_test.go`) that reproduce the exact original repro shape via mocks. `go build/vet/test ./...` all pass. Committed. Live batch (PID 2129285) still running and being monitored for #8/#10/#14.

- 2026-07-23 10:30 — User said to do what I recommended. Marked **bug #4 Resolved (2026-07-23)** in `bugs.md` (row, detail-section header, and a closing note explaining the unit-test-in-lieu-of-live rationale), and updated the Usability Gate's open-Major-bug bullet and overall status line: only #8, #10, #14 remain open now. Committed. Not pushed yet — this task (the live-batch verification / gate work) is still open; push happens at final close per the Working Protocol. Live batch still running (PID 2129285), still watching.

- 2026-07-23 10:35 — User asked if there was anything else to increase APPLIED chances. Ran a `job_funnel` outcome breakdown by source for the first time — found `BLOCKED_CAPTCHA` at 89% (Greenhouse), 91% (Lever), 96% (Ashby), 82% (Workable), all platforms previously assumed reliable. Live-probed real postings and confirmed genuinely fillable forms (21-40 real fields) were being killed by `isCaptchaBlocked` purely because a standard invisible reCAPTCHA/hCaptcha anti-spam widget iframe was present, not an actual block. **Filed and fixed as bug #45 (Blocker)** — `isCaptchaBlocked` now only trusts the challenge-frame-host signal when the main page doesn't already have a real form (`captchaWidgetFieldThreshold`); genuine DataDome-style interstitials (~0 real fields) still caught correctly. 3 new tests, full suite passes. Committed. User approved restarting the live batch with this fix: killed PID 2129285 cleanly (needed `-9`, confirmed dead via `ps aux`), built a fresh binary (`/tmp/career_agent_bin_live2`), launched as PID 2165142 (sole instance, confirmed via log growth and `ps aux`), re-armed the monitor. Baseline still 46 `APPLIED` at restart. This fix is likely to be what finally moves `APPLIED` and organically re-exercises #8/#10/#14 — watching closely.

- 2026-07-23 10:45-12:10 — User asked to push a job to the head of the queue to test the fix faster. The running batch had already loaded its queue snapshot into memory at startup, so true reordering wasn't possible without a restart; instead added a temporary `TARGET_JOB_URL` env-var filter to `cmd/agent/main.go` (reverted before committing, never shipped) to run one specific job in isolation using the real code path. User chose a full real run (real doc tailoring + real submit) over a dry run. Targeted a Lever posting (`jobs.lever.co/smarsh/...`) already confirmed live to have the false-positive shape. First attempt still failed instantly with the old "Security/Captcha block detected" message — found a **second, independent** CAPTCHA check in `cmd/agent/main.go` (raw `net/http` fetch, before fit-scoring) with the same bare-substring bug, filed and fixed as **bug #46**. Killed and restarted the main batch (PID 2165142 -> 2179450) with both fixes, reran the targeted test: this time it passed the description/CAPTCHA stages but failed at `handleLever` with `form failed to render in time` — a **third** gap: the dedicated Greenhouse/Lever handlers were never wired to bug #8's click-to-reveal step. Filed and fixed as **bug #47**. Paused the main batch (`kill -STOP`) to remove Ollama contention, reran the targeted test a third time: **success** — `Clicked an Apply-labeled element` -> `Detected Lever ATS. Filling out fields...` -> `job_funnel.status = APPLIED`. First fresh, real `APPLIED` produced since this whole effort began 2026-07-21. Resumed the main batch (`kill -CONT`). Updated bugs.md #45/#46/#47 with live verification and **checked the Usability Gate's live-batch checkbox** — MET as of 2026-07-23 12:00. Committed both the code fixes and the docs updates separately. Deleted the `applied_jobs` dedup row and reset `job_funnel.status` to `DISCOVERED` twice during retries (legitimate test retries, not real duplicates, since the earlier attempts never reached a real submit). Cleaned up stray monitors/log files along the way.

**Where this leaves the gate:** 5 of 6 boxes checked. Only remaining condition: zero open Major bugs (#8, #10, #14) — all three should now get real, organic exercise from live traffic for the first time, since #45/#46 were very likely also suppressing whatever jobs would have hit their paths. Main batch (PID 2179450) running unattended with all of today's fixes; still being monitored.

- 2026-07-23 20:15-21:17 — User asked why no email confirmation receipts were arriving for today's real `APPLIED` jobs. `cmd/tracker` only classifies REJECTED/INTERVIEW_REQUESTED, no "received" category, so a scan finding nothing proved nothing. Searched the connected Gmail account directly and found real evidence: a genuine Lever rejection (Avive Solutions) and Workable confirmation+rejection (ZeroFox) for real applications — auto-submit IS reaching employers. Nearly every job-related email (Lever, Workable, LinkedIn) was sitting in Trash — `pkg/tracker` has no delete logic, so that's a Gmail-side filter outside this repo, flagged to the user rather than "fixed." While investigating, found a real code issue anyway: `AttemptSubmit`'s only post-submit success signal was `currentURL != applyURL`, equally true for a validation-error redirect as a real success. **Filed and fixed as bug #51** — new `isSubmissionConfirmed` prefers explicit confirmation wording on page content, falls back to URL-looks-like-confirmation, only falls back further to bare URL-changed when no error wording is present. 5 new tests, full suite passes, committed. Rebuilt and restarted the batch (PID 2475804 -> 2542429) to pick up the fix; confirmed sole instance, re-armed the monitor with baseline 60 `APPLIED`. Pushed all 22 commits to `origin/main` at the user's request.

- 2026-07-23 18:51-19:08 — User asked to dig into why Ashby/Workable/Homerun sat at 0 `APPLIED`, keeping the main batch running throughout (read-only probes only, no interference). Also reported: 11 real `APPLIED` today (1 targeted test at 12:00:57 EDT, 10 organic from the unattended batch between 14:02:45 and 18:37:19 EDT — ~4h35m for 10, ~27-28 min average). Found via live probes: **Workable is account-gated on every posting checked** (0/12 real attempts, "log in" wall before any form field) — same structural class as Workday (bug #18), filed and fixed as **bug #50** (added to `authGatedATSHosts`, moved to priority tier 3). **Ashby and Homerun are NOT structurally broken** — a first too-fast probe falsely read Ashby as having no form; a proper wait+click found a real 12-field form. Their failures are Learner Module mapping/fill quality, the same open territory as #8/#10/#14. Also found 2 Homerun `FAILED_SUBMIT` rows were stale pre-fix junk-URL data (`IsKnownJunkJobURL` already filters these correctly — no code change needed, avoided a redundant fix). 4 new tests, full suite passes, committed. Rebuilt and restarted the batch again (PID 2327412 -> 2475804) to pick up the Workable fix; confirmed sole instance, re-armed the monitor with baseline 57 `APPLIED`.

- 2026-07-23 14:20-14:25 — With the priority queue live, a real Greenhouse posting (`job-boards.greenhouse.io/alphasense`) reached `handleGreenhouse` for the first time, filled every field successfully, then failed at the final submit click: `input#submit_app` (the only selector ever tried) has zero matches on this modern-board posting. Probed live and found the real control is an unidentified `<button type='submit'>`. Filed and fixed as **bug #49** — falls back to `button[type='submit']` only when the legacy selector has zero matches, so older-theme postings are unaffected. 2 new tests, full suite passes, committed. Requeued just the one diagnosed job (`cmd/requeue -pattern ... -status FAILED_SUBMIT -confirm -clear-dedup`, deliberately not the other 13 Greenhouse `FAILED_SUBMIT` rows, since those may have unrelated causes #49 doesn't address). Rebuilt and restarted the batch again (PID 2294955 -> 2327412) to pick up the fix; confirmed sole instance, re-armed the monitor with baseline 48 `APPLIED`. First-known live confirmation of bug #49's fix (job reaching a real submit attempt) still pending.

- 2026-07-23 13:20-13:36 — User asked about prioritizing the queue by success likelihood and rescoring as fixes ship. Built both: (1) extended `GetDiscoveredJobs`'s `ORDER BY` in `pkg/storage/manager.go` with a `sourcePriorityCASE` tiering Greenhouse/Lever first (proven working today), Ashby/Workable/Pinpoint/Homerun next, everything else, Workday last (unchanged); (2) new `cmd/requeue` CLI wrapping the manual SQL used earlier today — reports per-source outcome stats and, on `-confirm`, resets stale `BLOCKED_CAPTCHA`/`FAILED_SUBMIT` rows back to `DISCOVERED` (with `-clear-dedup` for the `applied_jobs` gap found earlier). New `SourceOutcomeBreakdown`/`RequeueByURLPattern`/`ClearApplicationRecordsByURLPattern` storage functions, all with test coverage; `cmd/requeue`'s pure pattern-resolution logic also tested. `go build/vet/test ./...` all pass. Manually verified against the real DB (dry run + `-stats`) before committing. Also found, while filing bug #48, an unrelated new live failure: a job hit Ollama's 6144-token context limit with an 11568-token validation-retry prompt (`exceed_context_size_error`) — noted but not investigated this session. Since the priority ordering only applies at process startup, restarted the batch again (PID 2253732 -> 2294955, same no-restart-needed binary otherwise) so it would actually pick up the new ordering for the 830 just-resurrected jobs; confirmed sole instance, re-armed the monitor, baseline still 47 `APPLIED`.

- 2026-07-23 12:35-12:45 — User asked whether frequent-failure sources should be quarantined. Live DB query showed `BLOCKED_CAPTCHA` counts for Greenhouse/Lever/Ashby/Workable were unchanged from before today's fixes — turned out to be stale, not current: `GetDiscoveredJobs` only pulls `status='DISCOVERED'`, so the hundreds of jobs wrongly blocked *before* #45/#46 shipped were never going to be retried automatically. Recommended requeuing them instead of quarantining anything (small-volume `applytojob.com` failures noted but not enough volume to justify excluding). User approved. Reset 830 rows (`ashby` 162, `greenhouse` 351, `lever` 263, `workable` 54) from `BLOCKED_CAPTCHA` back to `DISCOVERED`. Since the running process had already loaded its queue snapshot at startup, restarted it again (PID 2179450 -> 2253732, same binary, no code changes) so it would re-query and pick up the resurrected backlog. Confirmed sole instance via `ps aux` and log growth; re-armed the monitor with baseline 47 `APPLIED`. Watching for this large batch of previously-wrongly-blocked real jobs to start converting to genuine `APPLIED`.
