# Task Journal: Verify bug #4 (iframe form-fill fix) via live cmd/agent batch run

## Summary

- **Task:** bugs.md #4 — AttemptSubmit form-fill logic never looked inside iframes. Fix applied 2026-07-20 (`resolveFillTarget` in `pkg/submitter/browser.go`), unverified live. Working this run to reach a genuine `APPLIED` status and close the Usability Gate's live-batch checkbox.
- **Status:** In progress
- **Started:** 2026-07-21
- **Agent and model:** Claude Code / Sonnet 5 (orchestrator only — no code changes expected unless the live run surfaces a new defect)

## Pre-Flight Re-Evaluation

- **Usability Gate check:** NOT MET (bugs.md). This is exactly the gate's own "Next action" item, so it's in scope ahead of anything in improvements.md.
- **Model choice:** No delegation needed — this task is running the existing built binary live and observing behavior, not writing new code. If the run surfaces a genuine new defect requiring a code fix, will delegate that fix per the Working Protocol (agy headless first, Ollama for small bits).
- **Skills routed:** none needed yet (no code changes planned); will route `defensive_debugging` / `quality_assurance` if a new failure needs diagnosis.
- **Code re-verified:** confirmed `resolveFillTarget` fix is present in `pkg/submitter/browser.go` per bugs.md #4 detail section (not re-read line by line yet — will do so if a new form-fill failure appears).

## Pre-existing state found at session start

- Found a long-running Claude session (PID 79952, alive since 2026-07-19) with background jobs (dashboard on :8080, `podman exec ... tail -f career_agent.log`) still attached to this repo. Investigated: no `cmd/agent`/`go run` process actually running inside the `career-agent` podman container, `ollama ps` showed zero loaded models, and `career_agent.log` inside the container hadn't advanced since 2026-07-20 22:49:35 — confirmed idle, not a live conflict. User confirmed "proceed anyway" after this check.
- Untracked `agent_run_batch.log`/`agent_run_batch.pid` in repo root: leftover from a run attempted directly on the **host** (not the `career-agent` container), which hit the already-documented/Resolved Playwright missing-library issue (`libicudata.so.74` etc.) and never got past the Playwright install-check. Not a new bug — just confirms the fix for that resolved bug is container-only and someone (or an earlier attempt) ran on host by mistake. Left the files in place (untracked, harmless); will not commit them.
- `auto_submit_click: true` and `headless_browser: true` in `profile.yaml` — confirmed with user via AskUserQuestion that a real live run (real submissions to real job postings) is authorized for this session before proceeding.
- `.env` has no `LLM_PROVIDER` or `OLLAMA_TIMEOUT_MINUTES` set, so both fall back to their fixed defaults (`ollama` / 1 worker, 45-minute timeout) from bugs.md #6's fix — correct, no override needed.

## Plan

- [ ] Launch `cmd/agent` live inside the `career-agent` podman container (correct env per the resolved host-vs-container bug), capturing full log output
- [ ] Monitor for the run to reach `AttemptSubmit` on a real job and specifically watch for iframe-embedded-form cases (SmartRecruiters-style)
- [ ] Confirm at least one job reaches genuine `APPLIED` status in `applications.db` / dashboard metrics
- [ ] If a new failure appears, diagnose root cause before assuming it's bug #4 again (per this file's own history, "looks like #4/#3 again" has been a wrong diagnosis more than once)
- [ ] Update bugs.md: close #4 if verified, update Usability Gate checklist, file any new findings
- [ ] Run `go build/vet/test ./...` if any code changed
- [ ] Commit, delete this journal, push

## Progress Log

- 2026-07-21 — Journal opened. Pre-flight checks complete (concurrent-session check, host secrets noted but not touched, auto-submit risk confirmed with user). About to launch the live batch.
- 2026-07-21 ~10:15 — Launched `go run cmd/agent/main.go` inside the `career-agent` podman container (correct Go 1.26.5 via `/usr/local/go/bin` prepend). Initial monitor watched the wrong file (stdout redirect only captures `fmt.Printf` lines; all SRE `log.Printf` output goes to a lumberjack-rotated `career_agent.log` set via `log.SetOutput` in `cmd/agent/main.go:40`) — corrected to tail `career_agent.log` instead.
- 2026-07-21 ~11:20-11:47 — Live run active and healthy: 24+ API calls, multiple jobs reaching `AttemptSubmit`. Several jobs correctly blocked by the prompt-injection security guardrail (working as intended, not a bug). Observed 5 consecutive `developer.workday.com` doc pages being scored/tailored/attempted — live reproduction of bugs.md #5.
- 2026-07-21 ~11:47-12:10 — Used the wait time productively: diagnosed #5's root cause in `pkg/scraper/funnel.go` (`isValidATSUrl` bare `"workday.com"` suffix match), delegated the fix to Gemini 3.1 Pro via `agy`, verified the real diff against `git diff` (matched brief exactly, no fabrication), ran `go build/vet/test ./...` (all pass), updated `bugs.md` (moved #5's Workday/Workable portion to Resolved, filed remaining Greenhouse case as new #7), committed as `069ed98`. This did not touch the still-running live batch (separately compiled `go run` process in the container, unaffected by source edits on host).
- Still watching for bug #4: no iframe-embedded SmartRecruiters case or genuine `APPLIED` status reached yet in this run.

## Progress Log (continued)

- 2026-07-21 ~12:30-13:00 — SmartRecruiters case reached in the live run (`sosi1`), the exact ATS bug #4 was diagnosed against. Failed the same `failed to fill first_name` way. Investigated: no `using embedded iframe` log line fired, and `resolveFillTarget` runs *minutes* after page load (after the 5-10 min doc-generation call), ruling out a slow-render race. Direct `curl` inspection of both this SmartRecruiters posting and the earlier Breezy case both showed zero `<form>`/`<iframe>` tags and one unrelated stray `<input>`. Root-caused as a new, distinct bug: forms gated behind a click-to-reveal "Apply" interaction that nothing in the code handles. Filed as bugs.md #8 (commit `510e0a9`), broadened with the second confirmed case (commit `a2c4e55`). Bug #4 remains genuinely unverified — no case has exercised the iframe-fallback path at all this session, but nothing contradicts the fix either.
- 2026-07-21 ~13:00 — User confirmed pivoting to fix #8 now (best-evidenced path to an actual `APPLIED`). Wrote delegation brief, sent to agy/Gemini 3.1 Pro — hit an account-wide quota error before writing anything (git status stayed clean). Retried with agy/GPT-OSS 120B — returned exit 0 with a plausible summary, but `git diff` showed a broken edit (duplicated `resolveFillTarget` body, stray extra `}`, would not compile). Reverted (`git checkout --`) and applied the fix directly: added `clickApplyIfPresent(page)` in `pkg/submitter/browser.go`, called once in the Learner Module branch before DOM capture/fill. Added two unit tests in `browser_test.go`, working around a real Go embedding gotcha (`playwright.Locator`'s own `Locator(...)` method collides with an anonymously-embedded field of the same name — fixed via a type-alias embed). `go build/vet/test ./...` all pass. Committed `5afa230`, docs update `0023132`.
- 2026-07-21 ~13:15 — Killed the old (pre-fix) `go run cmd/agent/main.go` process in the `career-agent` container and relaunched with the fix compiled in (new PID). Re-armed the log monitor (the first one was collaterally killed by the `pkill -f` pattern matching its own script text — used a safer process-check pattern this time). Watching for the fix's effect on the next Learner-Module-routed job.
- Also noted but not yet investigated: a `jobs.smartrecruiters.com?keyword=remote` bare-domain search-query URL slipped through as a "job" (another FunnelEngine false positive, SmartRecruiters this time — candidate for #7's scope or a new row), and one Learner Module `failed to parse mapping json: invalid character 'T'` error (LLM returned non-JSON prose instead of the expected mapping JSON — a different, not-yet-filed issue). Worth filing before this session ends if time allows.

## Progress Log (continued 2)

- 2026-07-21 ~13:18-14:15 — Post-#8-fix run's next Learner Module case (Jobvite, `dwt`) still failed the same way, and `clickApplyIfPresent` never fired (no "Clicked an Apply" log line) — meaning it wasn't a #8 case at all. User confirmed writing a proper standalone diagnostic script (same method that cracked #4 originally). Wrote `tmp_diag/main.go` (temporary, deleted before commit, never tracked) — headless Playwright, same launch args/version as the app, navigates the exact failing URL, waits past networkidle + a settle period, dumps input/form/iframe/Apply-text-element counts + a screenshot. Ran it inside the `career-agent` container against `https://jobs.jobvite.com/dwt/job/o79Qzfwp/apply`: the job had expired and redirected to a `?error=404` page. Root-caused as a third, distinct, simple bug: the existing dead-job-phrase guard's exact-string match didn't cover this ATS's wording. Fixed directly (extracted `isDeadJobPage`, widened phrase list, added `TestIsDeadJobPage`), committed `ac12630`. Filed and resolved as bugs.md #9 (commit `797923d`), which also corrected #8's entry — the Jobvite case had been wrongly attributed to #8; #8's actual click-to-reveal fix still has no live evidence either way beyond the two pre-fix cases that motivated it.
- 2026-07-21 ~14:20 — Relaunched the live batch a third time with both #8 and #9 fixes compiled in (killed PID 876014's predecessor via `podman exec --user root ... kill -9`, needed `--user root` since the default exec session lacked permission to signal the root-owned process). Re-armed log monitor.

## Progress Log (continued 3)

- 2026-07-21 ~14:24-15:00 — With #8+#9 live: `clickApplyIfPresent` fired correctly on two real cases (Pinpoint/`hazelcast`, Breezy/`mind-computing`) — both then hit the prompt-injection guardrail on the revealed content, an expected security block, not a bug. Confirms the click mechanism itself works. Also hit: a Jobvite `/search` listing-page false positive (`cloudone-digital`, filed as #11) and a recurring `UNIQUE constraint failed: applied_jobs.url` on the same URL processed twice, seen on two different jobs now (filed as #12).
- 2026-07-21 ~15:05 — User asked for other approaches given the whack-a-mole pattern (3 distinct root causes behind the same symptom this session). Recommended and got approval for a structural fix: extend the existing but under-triggered Vision fallback (`AttemptVisionSubmit`, screenshot + `qwen2.5vl:7b` visual reasoning, already fully wired) to also fire on a DOM-mapped *fill* failure, not just an outright mapping-generation failure — since every failure diagnosed this session was the former. Implemented in `pkg/submitter/browser.go` (both the cached-mapping and fresh-Learner-Module-mapping paths). `go build/vet/test ./...` pass. Filed as bugs.md #10. Committed `9e8139b` (code) and `fae67b1` (docs, also filed #11/#12).
- 2026-07-21 ~15:08 — Relaunched the batch a fourth time (PID 922666) with #8+#9+#10 all compiled in. Re-armed monitor watching additionally for `Vision-Submit`/`Falling back to Vision`/`UNIQUE constraint` lines.

## Progress Log (continued 4)

- 2026-07-21 ~15:20-15:57 — Spent significant time investigating why Vision fallback (#10) appeared to never fire despite repeated fill failures (zero "Vision" occurrences anywhere in the log for a while). Re-read the code exhaustively (no shadowing, no logic bug found) and eventually concluded the confusion was likely caused by bug #12's duplicate-URL reprocessing scrambling the log's causal ordering for one specific job (`carrier.wd5.myworkdayjobs.com`), which was also observed hitting a live retry loop (4 failed attempts on the same URL within ~30 minutes, same `UNIQUE constraint failed: applied_jobs.url` error each time).
- 2026-07-21 ~15:55 — Mystery resolved empirically: on the very next Learner-Module case (`vetsez.breezy.hr`), both `[Auto-Submit] Falling back to Vision module` and `[Vision-Submit] Taking a full-page screenshot` fired cleanly — #10 does work correctly. The Vision call itself then failed due to a live Ollama OOM-kill (see below), not a #10 bug.
- 2026-07-21 ~16:02-16:05 — Investigated two Ollama connection-drop incidents (14:02, 15:57) with the user. Confirmed via `journalctl -k` these are genuine kernel OOM-kills of `llama-server` (13-15GB RSS each), not soft limits — this machine has 29GB total RAM. The second kill coincided with #10's Vision fallback first loading a second model concurrently. User approved mitigation: added `OLLAMA_MAX_LOADED_MODELS=1` to the systemd override (alongside #3's existing `OLLAMA_CONTEXT_LENGTH=6144`), restarted the service. Filed and documented as bugs.md #13 (Resolved/mitigated, not eliminated). Commit `a2e0232`. Batch process (PID 922666) was not restarted — it's continuing against the now-stable Ollama.

## Progress Log (continued 5)

- 2026-07-21 ~17:22-17:38 — User pushed hard on whether progress was real ("stabbing in the dark?") and pointed out `applied` metric hadn't moved. Checked directly: `applied` had been stuck at 35 for the entire ~7-hour session. Also spotted (during a routine tail) three near-simultaneous `ProcessJobApplication API Call #N` lines with different counters for the same company — proof of genuinely independent repeated pipeline runs on the same URL, not one run's log lines. Initially misread this as a multi-worker bug (10 workers); corrected after re-running the worker-label check properly date-anchored — an earlier unanchored `awk` filter had picked up stale entries from *yesterday's* pre-#3-fix session. Confirmed only `Worker-1` is active.
- 2026-07-21 ~17:38-17:45 — User approved fixing #12. Root-caused via direct code reading (`pkg/storage/manager.go`, `pkg/scraper/funnel.go`): `AddToFunnel`'s `ON CONFLICT ... DO UPDATE SET status=excluded.status` silently reset a job's funnel status back to `DISCOVERED` on every rediscovery (which happens constantly — same URLs rediscovered repeatedly across hours), and all 3 FunnelEngine call sites pushed to `jobChan` unconditionally regardless of insert-vs-update, so the same URL got queued and processed from scratch repeatedly. Very likely the dominant reason `applied` never moved all session.
- 2026-07-21 ~17:45 — Fixed directly (no delegation, well-understood small change): `AddToFunnel` now uses `ON CONFLICT DO NOTHING` + returns whether a row was newly inserted; call sites only queue on a genuine new insert. Added a regression test. `go build/vet/test ./...` all pass. Committed `847f0a8` (code) and `505dd9c` (docs, #12 marked Resolved). Killed the old process and relaunched (PID 1044458) with this fix live — the first run to have #8+#9+#10+#12 all together.

## Progress Log (continued 6) — major operational discovery

- 2026-07-21 ~18:00-18:35 — Relaunched-and-watched cycle appeared to show #12 still failing (same-looking "en"-labeled job hitting UNIQUE constraint repeatedly, ~5 min apart; one job, "skinspirit", logged 4 near-simultaneous "Initiating submission sequence" lines within 2 seconds). Spent significant time ruling out every code-level explanation: multi-worker (definitively ruled out via the authoritative `[Config] Using 1 worker(s)` startup log line), an outer retry wrapper (none exists — grepped, only one `AttemptSubmit` call site and one "Initiating submission sequence" log statement in the whole codebase), duplicate DB rows (only one `job_funnel` row existed for the URL), a cron/systemd daemon inside the container (none installed).
- 2026-07-21 ~18:35 — **Root cause found**: `ps aux` revealed five separate `/tmp/go-build*/b001/exe/main` processes still alive, one from *every* kill-and-relaunch cycle performed tonight (10:15, 13:15, 14:20, 15:07, 17:37). `go run` doesn't exec into its compiled binary — it stays alive as a wrapper around a separately-spawned child process. Every `kill -9 <go-run-pid>` this session killed only the wrapper, never the child, leaving that run's actual worker orphaned and still running. Five concurrent orphaned agent processes, all sharing `applications.db` and `career_agent.log`, fully explains the confusing symptoms: genuine cross-process concurrency despite each process correctly logging "1 worker", the skinspirit 4x burst, and very likely amplified both the Ollama OOM crashes and the earlier "Vision fallback isn't firing" mystery from earlier in the session.
- 2026-07-21 ~18:36 — Killed all five orphaned PIDs directly (`kill -9` on the actual `/tmp/go-build*/exe/main` PIDs, not the wrapper). Confirmed clean via `ps aux`. Relaunched properly this time: `go build -o /tmp/career_agent_bin ./cmd/agent` then ran that binary directly (PID 1099971) — no wrapper/child ambiguity, so any future kill is unambiguous. Documented the trap prominently in `bugs.md` (commit `9375fb5`) so no future session repeats it.
- This means tonight's live verification of #4, #8, #10, and #12 has been running in a contaminated (multi-process) environment for an unknown fraction of the session — the underlying code fixes are still believed correct (each was root-caused from direct evidence, not guessed), but a genuinely clean single-process observation window is needed to properly confirm any of them end-to-end.

## Progress Log (continued 7) — accessible-label fill fallback

- 2026-07-21 ~19:20-19:30 — With the clean single-process run active, watched two more real Vision-fallback cases reach `Gemini successfully mapped the visual DOM structure!` but still fail the actual fill (`empty selector provided for form filling` on one — the vision model returned no selector at all for first_name; `failed to fill first_name` on another after getting further, past phone, on the DOM-mapping attempt first). Confirms the remaining blocker is selector-guessing accuracy itself, not whether a fallback fires — both DOM and Vision mapping attempts are working mechanically but not reliably producing a correct selector.
- 2026-07-21 ~19:30 — User invited consulting other skills/sources. Checked the `accessibility` skill in `../ai_knowledge_library/.agents/skills/` (already globally available for Claude Code via `~/.claude/CLAUDE.md`, confirmed already wired for Career_Agent_Core specifically via its own `AGENTS.md` import too — told the user no new wiring was needed). It pointed at ARIA/label-based field identification as more robust than raw CSS selectors for third-party markup. Web search confirmed against current Playwright guidance: `GetByLabel`/`GetByRole` ("user-first locators") are the explicitly recommended primary strategy over CSS selectors for exactly this kind of unknown-markup automation.
- 2026-07-21 ~19:30-19:45 — Implemented: `GetByLabelLoc` added to the `fillTarget` interface (page + iframe), `FormMapping.Labels` field added, both Learner Module prompts (`pkg/mcp/client.go`) updated to also return each field's accessible label text, `safeFillWithLabelFallback` added (tries label first per best-practice ordering, falls back to the CSS selector guess). Wired into `handleDynamic`'s 4 PII fields. 4 new tests. `go build/vet/test ./...` pass. Committed `ab73fb5` (code), filed as bugs.md #14, committed `6775ec0` (docs).
- 2026-07-21 ~19:46 — Relaunched carefully this time: killed both the actual binary PID and its wrapper directly (not relying on `go run`'s ambiguous process tree), rebuilt fresh (`go build -o /tmp/career_agent_bin`), ran that binary directly (PID 1129548), verified with an anchored `pgrep -f '^/tmp/career_agent_bin$'` pattern this time to avoid the earlier self-match false-positive mistake. Re-armed monitor with the same anchored pattern plus a new `Label fill` grep term to catch the new fallback firing.

## Progress Log (continued 8) — #14 confirmed engaging, applied_jobs data verified real, new CSV logging feature

- 2026-07-21 ~19:55-19:56 — #14's label fallback confirmed engaging live: `[Auto-Submit] Label fill for "(586) 438-6057" failed...` — mechanism fires correctly, though this specific instance shows the vision model returned the phone *value* as the "label" (a prompt-following slip on the model's part, not a code bug). One data point, not yet a confirmed pattern.
- 2026-07-21 ~19:57 — User asked whether any of the 58 `applied` count are real successes. Verified directly: spot-checked the 8 most recent `job_funnel.status='APPLIED'` rows — all Greenhouse/Lever jobs (Reddit, Monzo, Akuity, You.com, etc., the platforms with dedicated hand-written handlers, not the Learner Module path). Confirmed real, complete, coherent tailored resume files exist on disk for several, dated **2026-07-15** — a week before this session. Answered honestly: the 58 are real but predate tonight; tonight's fixes (all in the Learner Module/Vision chain) haven't yet produced a new verified success of their own.
- 2026-07-21 ~19:58-20:15 — User asked to log any prompt-injection/hidden-content detections to a CSV (url, matched text, location, details). Implemented: `QuarantineLayer.CheckPayloadDetailed` (`pkg/security/filter.go`) exposes the underlying `promptsec.Threat` list instead of just a formatted error. Wired into `AttemptSubmit`'s live Learner Module branch only (traced all 3 `CheckPayload` call sites first — the other two check internal RAG output and dead/unused code, not scraped site content). `storage.LogPromptInjectionDetections` (`pkg/storage/manager.go`) appends one CSV row per threat to `applications/prompt_injection_detections.csv` via `encoding/csv`. 4 new tests. `go build/vet/test ./...` pass. Committed `a1cee53` (code), filed as improvements.md #19 Done, committed `dbf0a62` (docs).
- 2026-07-21 ~20:20 — Relaunched carefully again (killed the exact verified PID 1129548, rebuilt, ran the new binary directly as PID 1148751). Re-armed monitor with an added `Failed to log prompt injection` term (would only fire if the new CSV writer itself errors) plus everything from before.

## Progress Log (continued 9) — dashboard features, a second data-correction bug caught, docs refresh

- 2026-07-21 ~21:40-22:00 — User asked to show the last successful application (with date) plus a live activity indicator on the dashboard. Implemented in `cmd/dashboard`: extended `/api/metrics` to query the last genuinely-completed application (joining `applied_jobs` against `job_funnel.status IN ('APPLIED','PROCESSED_MANUAL')`, not `applied_jobs` alone), added a pulsing live/idle/offline status dot and an animated activity sweep bar. **Caught two real bugs while building this**, not hypothetical: (1) the first draft built the last-applied link via string-concatenated `innerHTML` using scraped `last_applied_url` data — an XSS risk given this app already fights prompt injection from the same untrusted-content class all session; rewrote using safe DOM construction (`createElement`/property assignment) plus a strict http(s)-only URL allowlist. (2) the dashboard surfaced a known FunnelEngine false positive (`cloudone-digital/search`, bug #11) as a "successful application" — structurally impossible. Traced this to a flaw in the earlier `#12` data correction: cross-checked all 58 `status='APPLIED'` rows against `execution_logs` (a real, populated audit trail distinct from the unused `execution_state` table) and found **12 of the 23 corrected rows had actually failed**, not succeeded. Reverted those 12; honest total is **46**, not 58. Documented fully in bugs.md. Committed `8661e62`, pushed.
- 2026-07-21 ~22:00-22:20 — User asked for a minimal "currently processing" indicator plus last skipped/failed with reasons. Required a real schema addition (`job_funnel.last_updated`, migrated via explicit `ALTER TABLE` since `CREATE TABLE IF NOT EXISTS` never touches an existing table — same lesson as other schema-adjacent findings tonight) so "currently processing" can distinguish the truly-active job from old orphaned `PROCESSING` rows (135 of those exist per earlier reconciliation). Wired into `UpdateFunnelStatus`/`UpdateFunnelStatusWithScore` using `time.Now()` as a bound parameter rather than SQLite's `CURRENT_TIMESTAMP` (always UTC, no offset) — caught this timezone inconsistency live by comparing the new timestamps against the existing (correct) `last_applied_at` display. Added a `statusReason()` mapping from raw status codes to human-readable text. 6 new tests. Committed `d57ef47`. Relaunched the agent twice more (once for the schema/dashboard-query change, once for the timestamp fix) — now PID 1205915, confirmed live and populating the new fields correctly.
- 2026-07-21 ~22:25 — User asked to correct `docs/index.html` (GitHub Pages source) - it still described a "Live TUI Dashboard", stale since well before this session (the project has run a web dashboard all along). Also refreshed `README.md`: several bullets still said "Gemini"/"Gemini 1.5 Pro" specifically despite Ollama being the actual default provider, the Auto-Submit Framework bullet said "currently targets LinkedIn Easy Apply" (stale - real architecture is dedicated Greenhouse/Lever handlers + the Learner Module built out this session), Go version said 1.21+ (go.mod pins 1.26.5), the Playwright install command referenced the dead `playwright-community/playwright-go` fork (fixed in code 2026-07-20 but never updated in the README), and `auto_submit: true` was referenced as a config field that doesn't exist (real field is `auto_submit_click`). Committed `561567f`, pushed.

## Next Step

Per explicit user instruction, back to active monitoring: watch the live batch (PID 1205915, all fixes #5 through #14 plus the dashboard/schema changes live) for `applied` movement, new #14/#16 label-fallback data points, and any other issues worth catching and fixing proactively as they surface — not just passively logging. #7/#11 (FunnelEngine search-page false positives for Greenhouse/Jobvite) and the intermittent `failed to parse mapping json: invalid character 'T'` error remain open, not yet filed with full rigor, and are reasonable next candidates if something concrete surfaces. Verify the dashboard (`localhost:8080`) continues showing accurate live data as the batch progresses.
