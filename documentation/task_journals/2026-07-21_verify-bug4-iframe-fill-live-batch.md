# Task Journal: Verify bug #4 (iframe form-fill fix) via live cmd/agent batch run

## Summary

- **Task:** bugs.md #4 — `AttemptSubmit` form-fill logic never looked inside iframes. Fix applied 2026-07-20 (`resolveFillTarget` in `pkg/submitter/browser.go`), closed 2026-07-23 via unit tests (see bugs.md #4's Details — live traffic can no longer organically exercise this path). Working live batches since 2026-07-21 fanned out into ~50 other real, independently-diagnosed bugs (#5 through #57), all detailed in `bugs.md` directly rather than here. **The Usability Gate reached MET 2026-07-24** once #8/#10/#14 (the gate's last remaining Major bugs) closed the same way #4 did (direct end-to-end test, live confirmation of the specific path structurally unreachable) — see Progress Log. This journal's remaining scope is now narrower: finish (or explicitly abandon) the 82-job historical re-verification run below, independent of gate status.
- **Status:** In progress (Usability Gate itself is MET; this journal tracks the still-open 82-job re-verification)
- **Started:** 2026-07-21
- **Agent and model:** Claude Code / Sonnet 5 (orchestrator; delegates real fixes per the Working Protocol when well-scoped)

## Pre-Flight Re-Evaluation (last done 2026-07-24, fresh `/resume_task` session, right after the ~17:25 clear)

- **Usability Gate check: MET as of 2026-07-24 ~15:50.** All six boxes are now checked — see below. This task's own remaining scope (the 82-job re-verification) is **not** what the gate was waiting on in the end; it's a separate, still-open effort that continues regardless (see Next Step).
- **Model choice:** No delegation needed for observation; code fixes so far have been split between direct edits (well-understood, small) and `agy` delegation (larger, clearly-scoped diffs, always verified against `git diff` before trusting). #8/#10/#14's closing tests (below) were written directly, not delegated, given the amount of specific context already gathered and this repo's own recorded history of a delegate producing a plausible-but-non-compiling diff for #8 the first time it was attempted.
- **Code re-verified 2026-07-24 ~16:00:** `go build/vet/test ./...` all pass clean. Today's #8/#10/#14 test additions are committed (`1006249`) and pushed to `origin/main` — working tree is clean except the untracked `applied_urls_verify82.txt` (expected, not meant to be committed).

## Progress Log (condensed — full blow-by-blow lives in bugs.md's Detail sections, not duplicated here)

- 2026-07-21 — Long live-triage session. Diagnosed and fixed #5 through #17. Five orphaned `go run` child processes found contaminating one observation window (Operational Trap, documented at the top of `bugs.md`) — cleaned up.
- 2026-07-22 — Further live triage (~6 hours): found and fixed a real environment regression (agent running on the bare Bazzite host instead of the `career-agent` distrobox), then a chain of real interaction bugs (#34-#38, #41-#44).
- 2026-07-23 — Priority-queue + requeue tooling shipped (`cmd/requeue`, source-priority `ORDER BY`). Two independent CAPTCHA-detection false positives found and fixed (#45 Blocker, #46 Blocker) — these had been killing the large majority of Greenhouse/Lever/Ashby/Workable jobs before fit-scoring or fill ever ran. Once cleared, #47 (dedicated Lever/Greenhouse handlers missing the click-to-reveal step) surfaced and was fixed. **First fresh, real `APPLIED` produced since this effort began**, via a targeted `TARGET_JOB_URL` test — Usability Gate's live-batch checkbox checked. Bug #4 closed via unit tests (structurally unreachable live — see above). #48-#51 found and fixed the same day (Greenhouse submit-selector fallback, Workable account-gating, and — most significant — #51: the post-submit success check trusted any URL change, not actual confirmation evidence).
- 2026-07-24 (overnight into today) — User asked to requeue all 82 previously-`APPLIED` jobs and re-verify them for real, after #53 found `isSubmissionConfirmed` (#51's fix) was structurally unreachable for every ATS except Lever/Greenhouse/LinkedIn — meaning most of the 82's original `APPLIED` status had zero confirmation evidence, ever. `TARGET_JOB_URL` extended to accept a comma-separated list, `cmd/requeue -status APPLIED` support added, both shipped before executing. All 82 reset `APPLIED` → `DISCOVERED`, isolated `TARGET_JOB_URL` run launched (self-terminating, does not pick up normal discovery — **the normal full-backlog batch must be manually restarted once this finishes**, see `bugs.md`'s Operational Trap notes for the restart procedure). Chain of real bugs found and fixed while watching this run: #52 (payload-size circuit breaker, recurred 3× with genuinely different root causes each time), #53 (confirmation logic unreachable for most ATS, Blocker), #54 (Ashby SPA false-positive), #55 (orphaned PROCESSING rows never retried), #56 (dashboard missing two status tiles), #57 (forms too large for Ollama's context window, routed to `MANUAL_REQUIRED` instead). 4 restarts total, each picking up one new fix. **Still 0/82 resolved as of this writing** — every real outcome so far has been a new bug found and fixed, not an answer to "were the original 82 real."
- 2026-07-24 ~15:20-15:30 (this session, after a `/clear`) — Resumed from journal, confirmed PID 3137654 alive and the 82-job breakdown unchanged (0 `APPLIED`), re-armed a monitor. User asked to add "time to process" (discovered_at → terminal-status duration) to the dashboard's Last Applied/Skipped/Failed/Manual cards, doubting the Manual Queue tile's accuracy. Implemented (`cmd/dashboard/main.go`: `formatDuration` helper + 4 new `*ProcessingTime` fields; `cmd/dashboard/index.html`: new "Time to process: …" line per card; `cmd/dashboard/main_test.go`: new tests including a NULL-`discovered_at` guard). **Confirms the user's suspicion**: the Manual Queue's driving row took 7d19h from discovery to `MANUAL_REQUIRED`, Last Skipped/Failed over 10d — the tiles are raw current-status counts, not "recently flagged," and this single-worker queue is genuinely that far behind on old backlog. Verified live via a rebuilt dashboard binary + a real screenshot (had to fall back to Firefox — this environment's Chromium build is missing `libicudata`/`libicui18n`/`libicuuc`/`libavif`, per the Playwright warning already logged at every `cmd/agent` startup, which silently zeroes out all text layout/`getBoundingClientRect()` results without erroring; Firefox has no such dependency gap here). `go build/vet/test ./...` all pass. Committed (`f43cc6c`) and pushed at the user's explicit request.
- 2026-07-24 ~15:35 (fresh session after another `/clear`) — Resumed from journal again. Verified against live state before acting, per protocol: `git status`/`git log` match exactly what the journal claims (only untracked `applied_urls_verify82.txt`, HEAD still `44d2ca6`), PID 3137654 and dashboard PID 3148044 both still alive, `bugs.md`'s #8/#10/#14 (Major, Pending) and #48 (Minor, Pending) unchanged. Outcome-breakdown query returned the exact same counts as the prior session's last read (`DISCOVERED=77 FAILED_SUBMIT=1 MANUAL_REQUIRED=1 PROCESSING=1 SKIPPED=2`) — no regression, just genuinely slow (one job at a time, ~15-20 min each per the log). Confirmed via log tail the run is healthy and progressing: Reddit resolved to `MANUAL_REQUIRED` at 15:30:25 exactly as the prior session recorded, now mid-fit-scoring on Akuity. `TaskList` showed no lingering monitor from the earlier session (the "stray" one noted below is gone). Re-armed a fresh persistent monitor (task `bf3w1kksp`) on the same poll-every-90s outcome-breakdown pattern. Nothing else to do until it reports a change — this is a waiting task, not a stuck one.
- 2026-07-24 ~15:45-15:55 (same session) — Answered a user question about the dashboard's "time to process" numbers (confirmed the ~10-day figures are genuine queue-wait time, not active processing time: the driving rows were all `discovered_at` 2026-07-13, only resolved tonight by this dedicated re-verification run — a single-worker/~3000-job-backlog artifact, not a bug). User then asked to resolve bugs #8, #10, and #14 (the Usability Gate's last remaining condition) and to journal status for resumability. Read `pkg/submitter/browser.go`/`vision.go`/`mcp/client.go`'s `FormMapper` interface and the existing all-mock unit-test style in `browser_test.go` (MockPage/MockFrame/MockLocator implementing the real `playwright-go` interfaces via embedding). Confirmed via `bugs.md`'s Details sections that all three bugs' fixes were already shipped and had each fired correctly live at least once, but none had ever landed on a page that was still fillable afterward (Workday's auth wall dominated every live trigger) — the same "mechanism confirmed, outcome unreachable via live traffic" shape bug #4 hit, which was closed via a direct unit test. Applied the same precedent: added `MockMapper` (implements `FormMapper`) and six new MockPage method overrides (`Goto`/`Route`/`SetDefaultTimeout`/`AddInitScript`/`Close`/`Screenshot` — never needed before since prior tests only reached the top of `AttemptSubmit`) to `pkg/submitter/browser_test.go`, then wrote two full end-to-end tests driving the real `AttemptSubmit` orchestration (not just isolated helpers): `TestAttemptSubmit_ClickToRevealPlusLabelFallback_EndToEndSuccess` (closes #8+#14: a mock page with zero form fields until an Apply click, deliberately wrong CSS selectors but correct labels in the Learner Module's mapping, confirms the click fires, the label fallback is what actually fills the fields, and the submission reaches a confirmed success) and `TestAttemptSubmit_VisionFallback_EndToEndSuccess` (closes #10: a mapping that fails to fill for real, confirms `ExtractFormMappingVision` is actually invoked with a real screenshot payload, and the vision-remapped fill reaches a confirmed success). Verified both by running them individually first (`-run` filter, `-v`, confirmed the exact expected log lines fired: "Clicked an Apply-labeled element", "Falling back to Vision module", "Submission confirmed... URL looks like a confirmation page"), then ran the full suite (`go build/vet/test ./...` from inside the `career-agent` distrobox via `/usr/local/go/bin/go` — the bare-host `go` and the container's default `/usr/bin/go` are both wrong/too-old for this repo's `go.mod`) — all pass. Updated `bugs.md`: all three Ranked Backlog rows marked Resolved with closing notes citing the new tests, each `### N. Title` heading given a `(Resolved 2026-07-24)` suffix matching the existing convention (see #4's heading for precedent), and the Usability Gate's 6th checkbox flipped to `[x]` with its Status line changed to `MET` — confirmed first that #48 (the only other Pending bug) is Minor and doesn't block that checkbox, and that no other Pending Blocker/Major rows existed anywhere else in the Ranked Backlog table. Committed (`1006249`) and pushed to `origin/main`.
- 2026-07-24 ~15:55 — Noticed three separate monitors (`bf3w1kksp` from this session, plus `budchyfjm` and `b1ojsq6q2` from earlier sessions that apparently survived past their originating `/clear`, contradicting this journal's own earlier assumption that monitors don't survive a clear) all fired on the same Akuity→`MANUAL_REQUIRED` status change within seconds of each other. Not yet consolidated — see Next Step.
- 2026-07-24 ~16:05 (fresh session after another `/clear`) — Resumed from journal, verified all claims against live state before acting: `git status`/`git log` match exactly (only untracked `applied_urls_verify82.txt`, HEAD `5fc4a31` with `1006249` present, `origin/main` matches). `TaskList` showed **zero** surviving monitors this time (all three from the prior session, including the supposedly-just-armed `bf3w1kksp`, are gone) — reconfirms monitors do not reliably survive a clear; don't assume otherwise going forward. PID `3137654` alive (47m elapsed) and dashboard PID `3148044` alive (34m elapsed), both confirmed via `ps -p`. Outcome-breakdown query returned `DISCOVERED=76 FAILED_SUBMIT=1 MANUAL_REQUIRED=2 PROCESSING=1 SKIPPED=2` — identical to the prior session's 16:00 read, confirming Akuity's `MANUAL_REQUIRED` (bug #57's fix) already landed and nothing regressed. Log tail shows real progress since the prior session's last observation though: Akuity resolved to manual-required at 15:46 exactly as expected, and the run has since moved on to You.com (fit-scored 80, currently mid-`ProcessJobApplication` Ollama call since 15:53). Re-armed a fresh monitor (task `b6nl5hsv3`, 90s poll, same pattern as before) — no other monitor found to consolidate with this time. Nothing else to do; this remains a waiting task, not a stuck one.
- 2026-07-24 ~16:10-17:20 (same session) — User asked what else was open; per `improvements.md`'s Working Protocol (gate is MET, so its ranked table applies), worked item **#15 (email/portal conversion-rate analytics)** end to end in parallel with this journal's own waiting task — that work has its own commits and is fully closed, not tracked further here (see `improvements.md`'s #15 entry for the Done note). Worth recording here only because of one operational finding: Gemini quota was **fully exhausted across all three tiers** (`gemini-3.1-pro-high`, `-low`, `gemini-3.6-flash-high` all returned "Individual quota reached... Resets in 94h") — stepped to `gpt-oss-120b-medium` per the delegation protocol, which worked (different quota pool) but only echoed back a restated plan and asked already-answered questions rather than implementing (no way to respond in headless mode). Implemented #15 directly instead. **Record for any future delegation this week: don't retry Gemini tiers before ~2026-07-28 ~14:30 (94h from 2026-07-24 ~16:15) — go straight to `gpt-oss-120b-medium` or local Ollama.** Also rebuilt the dashboard binary to `/tmp/career_dashboard_v3` (supersedes `/tmp/career_dashboard_v2`) with #15's new conversion-rate card/table baked in; restarted cleanly, confirmed live against the real DB (still 0 tracked applications — the 82-job run hasn't produced a fresh `APPLIED`/`REJECTED`/`INTERVIEW_REQUESTED` row yet, consistent with this journal's own tracking below).
- 2026-07-24 ~17:20-17:25 (same session, wrapping up before a clear) — Mid-#15's verification work, `TaskList` unexpectedly showed a **live, still-running** stray monitor `bf3w1kksp` from the *2026-07-24 ~15:35* session (it fired a duplicate notification identical to `b6nl5hsv3`'s). This directly contradicts the ~16:05 entry above, which found zero strays and concluded "monitors do not reliably survive a clear" as if that were now settled — it isn't. **Corrected understanding: monitor survival across `/clear` is inconsistent/unpredictable, not reliably one way or the other.** Always run `TaskList` on resume and actually look for strays; don't assume either outcome. Stopped `bf3w1kksp` via `TaskStop` (confirmed via its returned command that it was genuinely still polling PID 3137654). Only `b6nl5hsv3` remains armed. Re-verified full outcome-breakdown and PID liveness before this update (see Next Step) — no regression, run is healthy.
- 2026-07-24 (fresh session, `/resume_task`) — Verified all journal claims against live state before acting: `git status` matches exactly (only untracked `applied_urls_verify82.txt`), `git log` HEAD is `84a9e3d` (one journal-refresh commit ahead of the `0092555` the journal cited — expected, not a red flag), `origin/main` matches HEAD. PID `3137654` alive (elapsed 1h17m20s) and dashboard PID `3199531` alive (elapsed 6m25s, serving HTTP 200 on :8080). `TaskList` showed **zero** surviving monitors this time (neither `b6nl5hsv3` nor any other) — reconfirms the prior entry's point that survival is inconsistent, not a new data point either way. Outcome-breakdown query returned `DISCOVERED=75 FAILED_SUBMIT=1 MANUAL_REQUIRED=3 PROCESSING=1 SKIPPED=2` (sums to 82) — identical to the last recorded read, but log tail shows real, healthy progress underneath: Akuity and You.com both resolved to `MANUAL_REQUIRED` as previously recorded, and the run has since moved on to "Backend Software Engineer" (enveritas, Greenhouse), fit-scored 85, currently mid-`ProcessJobApplication` Ollama call — well within the ~15-20 min normal duration for this call, not a stall. Re-armed a fresh monitor (task `b0s0lnzce`, 90s poll, same pattern as documented below). Nothing else to do; this remains a waiting task, not a stuck one.
- 2026-07-24 (same session, `/groom_backlogs`) — Ran a full groom pass on `bugs.md`/`improvements.md` in parallel with this journal's own waiting task; unrelated to the 82-job run itself but worth recording here since it landed a real commit on top of this journal's own HEAD. Closed `bugs.md` #48 (Lever click-to-reveal staleness theory) — the 82-job run itself supplied the disconfirming evidence: 11+ further Lever postings processed 2026-07-24 alone, each under the same long-doc-gen-wait precondition #48 theorized, zero recurrence of the original failure. Also re-scored `improvements.md` #11 below the ROI floor and un-deprioritized #22 (the Usability Gate it was waiting on is MET). All committed and pushed as `9d62907`, on top of this journal's own `5e29a34`. Re-verified after: `git log` HEAD `9d62907`, `git status` still only the expected untracked `applied_urls_verify82.txt`. PID `3137654` still alive (elapsed 1h26m08s), outcome breakdown unchanged (`DISCOVERED=75 FAILED_SUBMIT=1 MANUAL_REQUIRED=3 PROCESSING=1 SKIPPED=2`), log tail confirms still on the same healthy `ProcessJobApplication` call for "Backend Software Engineer" (enveritas) begun 16:19:40, not stalled. `TaskList` showed the `b0s0lnzce` monitor from earlier this session had already died (same unpredictable-survival pattern, this time within the same session rather than across a clear) — re-armed as `bl5mmg1dw`. **A fresh session after this point should expect `git log` to show `9d62907` as an unrelated-but-legitimate commit ahead of this journal's own state — not evidence of anything going wrong.**
- 2026-07-24 ~16:35-16:40 (fresh session, `/resume_task`) — Verified all journal claims against live state before acting: `git status` matches exactly (only untracked `applied_urls_verify82.txt`), `git log` HEAD is `f37d680` (one journal-refresh commit ahead of the `9d62907` the journal last cited — expected per that entry's own note, not a red flag), `origin/main` matches HEAD. PID `3137654` alive (elapsed 1h28m20s) and dashboard PID `3199531` alive (elapsed 16m59s, confirmed serving HTTP via `ss -tlnp | grep 8080`). Outcome-breakdown query returned `DISCOVERED=75 FAILED_SUBMIT=1 MANUAL_REQUIRED=3 PROCESSING=1 SKIPPED=2` — same totals as the last recorded read, but log tail confirms real progress underneath: Akuity and You.com (both previously "PROCESSING") have since resolved to `MANUAL_REQUIRED` via the "form too large for local model" path (bug #57's fix, going through one validation-retry attempt first — expected mechanism, not a new bug), accounting for the count already reflected. Run has moved on to "Backend Software Engineer" (enveritas, Greenhouse), fit-scored 85, mid-`ProcessJobApplication` Ollama call since 16:19:40 (~19min elapsed at check time — within the documented 15-20min normal range, not stalled). `TaskList` showed **zero** surviving monitors (`bl5mmg1dw` gone) — reconfirms survival is unpredictable, consistent with prior entries. Re-armed a fresh monitor (task `bu7k9xvol`, 90s poll, same pattern as documented below). Nothing else to do; this remains a waiting task, not a stuck one.

## Next Step (accurate as of 2026-07-24 ~16:40)

**Bugs #8/#10/#14's closure and improvement #15's shipment are both fully committed and pushed** — nothing left to do for either. Latest `origin/main` HEAD is `0092555` (#15, conversion-rate analytics), on top of `89f7e2f`/`11a3904` (journal-only) and `1006249` (#8/#10/#14). `go build/vet/test ./...` all pass (re-verified 2026-07-24 ~17:20). **A fresh session should NOT redo any of this** — if `git log` doesn't show `0092555`, treat that as a red flag and re-check before assuming the work was lost.

**The Usability Gate is MET (flipped 2026-07-24 ~15:50, committed) — but this task itself is NOT done.** The gate's "zero open Blocker/Major bugs" box didn't need the 82-job run to finish; it needed #8/#10/#14 closed, which happened via direct test (same precedent as #4) rather than via this run producing a fresh `APPLIED` through those specific paths. The 82-job re-verification is a separate, independently-valuable effort (confirming the *historical* 82 `APPLIED` rows were genuine, per bug #53's finding that most had zero confirmation evidence ever) that continues regardless of gate status. Don't treat "gate is MET" as a reason to stop watching it.

**Core unanswered question for the 82-job run (why it's still open):** user asked to requeue all 82 previously-`APPLIED` jobs (39 Lever, 32 Greenhouse, 7 Workday, 2 SmartRecruiters, 2 Pinpoint) and re-verify them for real now that bug #53's confirmation-logic fix is live. **As of this writing, 0 of the 82 have reached a confirmed `APPLIED` or a genuine duplicate-block result** (2 have reached `MANUAL_REQUIRED`, a legitimate terminal state per bug #57's fix, not a failure). Don't declare this task done just because bugs stop surfacing — it's done when the 82 (or as many as reach a terminal state) show real evidence one way or the other. See bugs.md's #52/#53 Details sections for the full diagnostic history if more context is needed; it is not repeated here.

**Live process running right now, independent of any chat session:** PID `3137654` (`/tmp/career_agent_bin_verify82d`, has every fix through bug #57), started 2026-07-24 ~15:10, running inside the `career-agent` distrobox. Confirm alive: `distrobox enter career-agent -- ps -p 3137654`. Tail: `distrobox enter career-agent -- tail -f career_agent.log`.

**Outcome-breakdown query** (82-URL list at `applied_urls_verify82.txt`, repo root, untracked — don't delete, needed for this query):
```bash
urls_file="/var/home/howlcipher/dev/Career_Agent_Core/applied_urls_verify82.txt"
in_clause=$(awk '{printf "%s%s%s", (NR>1?",":""), "\x27", $0"\x27"}' "$urls_file")
distrobox enter career-agent -- sqlite3 /var/home/howlcipher/dev/Career_Agent_Core/applications.db \
  "SELECT status, COUNT(*) FROM job_funnel WHERE url IN ($in_clause) GROUP BY status ORDER BY status;"
```
Last read (this session, ~16:40): `DISCOVERED=75 FAILED_SUBMIT=1 MANUAL_REQUIRED=3 PROCESSING=1 SKIPPED=2` (sums to 82, unchanged in totals from the prior session's last read, but log tail confirms real underlying progress since then — Akuity and You.com both resolved PROCESSING → `MANUAL_REQUIRED` via bug #57's fix. Run has since moved to "Backend Software Engineer" (enveritas), mid-`ProcessJobApplication`). PID `3137654` confirmed alive this session (elapsed 1h28m20s at check time).

**Dashboard binary is `/tmp/career_dashboard_v3`, PID `3199531`** (supersedes `/tmp/career_dashboard_v2`/PID `3148044`). Confirmed alive this session, serving HTTP 200 on :8080. If it needs restarting: `distrobox enter career-agent -- bash -c "cd /var/home/howlcipher/dev/Career_Agent_Core && /usr/local/go/bin/go build -o /tmp/career_dashboard_v3 ./cmd/dashboard && nohup /tmp/career_dashboard_v3 > /tmp/dashboard.log 2>&1 & disown"`. Check first with `ss -tlnp | grep 8080` before rebuilding.

**Monitor survival is unpredictable — do not assume either outcome, and don't assume it only varies across a `/clear`.** Confirmed again this session: `TaskList` was empty on resume (no `bl5mmg1dw` survivor). **Always run `TaskList` before trusting a previously-armed monitor is still working — don't skip it because a previous check (in this session or a past one) found it present or absent once.** Currently armed: `bu7k9xvol` (its first notification, a baseline `STATUS CHANGE` with the same totals as above, fired immediately on arming since `prev` starts empty — that's expected, not a real change; only treat later notifications with *different* totals as real). If `TaskList` shows it still present, no need to re-arm — just confirm it's still polling correctly. If it's gone, re-arm with the pattern below.
```bash
urls_file="/var/home/howlcipher/dev/Career_Agent_Core/applied_urls_verify82.txt"
in_clause=$(awk '{printf "%s%s%s", (NR>1?",":""), "\x27", $0"\x27"}' "$urls_file")
pid=3137654
prev=""
while true; do
  if ! distrobox enter career-agent -- kill -0 "$pid" 2>/dev/null; then
    echo "PROCESS $pid IS NO LONGER RUNNING -- run may have completed or crashed."
    break
  fi
  cur=$(distrobox enter career-agent -- sqlite3 /var/home/howlcipher/dev/Career_Agent_Core/applications.db \
    "SELECT status || '=' || COUNT(*) FROM job_funnel WHERE url IN ($in_clause) GROUP BY status ORDER BY status;" 2>/dev/null | tr '\n' ' ')
  if [ "$cur" != "$prev" ]; then echo "STATUS CHANGE: $cur"; prev="$cur"; fi
  sleep 90
done
```
Don't kill/restart PID 3137654 just to attach a fresh monitor — that only loses progress.

**IMPORTANT — this is NOT the normal full-backlog batch.** It's a dedicated, isolated `TARGET_JOB_URL` run restricted to exactly the 82 URLs. It is self-terminating: once all 82 resolve, the process exits on its own and does **not** pick up fresh discovery or the rest of the ~3000-job backlog. **After this run finishes (or if you decide to abandon it), the normal batch must be manually restarted** — build `cmd/agent` fresh, launch WITHOUT `TARGET_JOB_URL` set, confirm sole instance, re-arm a normal `APPLIED`-count monitor. See the Operational Trap notes at the top of `bugs.md` for the full restart procedure.

**A duplicate-application nuance to remember:** the user explicitly does not want "the ATS correctly rejected a re-submission as a duplicate" treated as a bug — that's actually confirming the original was genuine. Check the real failure reason/page content (not just "FAILED_SUBMIT") before concluding anything's wrong.

**Environment note for any future visual/Playwright verification:** this machine's Chromium (via `mxschmitt/playwright-go`) is missing `libicudata.so.74`/`libicui18n.so.74`/`libicuuc.so.74`/`libavif.so.16` — pages load and JS runs fine, but **all text has a zero-size layout box** (`getBoundingClientRect()` returns 0×0, `innerText` is empty) with no error surfaced anywhere. A screenshot taken with Chromium will look visually broken/blank of text even when the page is correct. Use `pw.Firefox.Launch()` instead (confirmed working 2026-07-24) until/unless those system libs get installed.

**The Usability Gate is already MET (see above) — that is no longer this task's closing condition.** This task is done once the 82-job run resolves (or is explicitly abandoned) and its outcome — real evidence on whether the historical 82 `APPLIED` rows were genuine — is recorded (in `bugs.md` if it surfaces a new bug, or just noted here if the run simply finishes cleanly). Delete this journal in that final commit.
