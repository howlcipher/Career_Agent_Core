# Task Journal: Verify bug #4 (iframe form-fill fix) via live cmd/agent batch run

## Summary

- **Task:** bugs.md #4 — `AttemptSubmit` form-fill logic never looked inside iframes. Fix applied 2026-07-20 (`resolveFillTarget` in `pkg/submitter/browser.go`), closed 2026-07-23 via unit tests (see bugs.md #4's Details — live traffic can no longer organically exercise this path). Working live batches since 2026-07-21 fanned out into ~59 other real, independently-diagnosed bugs (#5 through #59), all detailed in `bugs.md` directly rather than here. **The Usability Gate reached MET 2026-07-24** once #8/#10/#14 (the gate's last remaining Major bugs) closed the same way #4 did (direct end-to-end test, live confirmation of the specific path structurally unreachable) — see Progress Log. This journal's remaining scope is now narrower: finish (or explicitly abandon) the 82-job historical re-verification run below, independent of gate status.
- **Status:** In progress (Usability Gate itself is MET; this journal tracks the still-open 82-job re-verification)
- **Started:** 2026-07-21
- **Agent and model:** Claude Code / Sonnet 5 (orchestrator; delegates real fixes per the Working Protocol when well-scoped)

## Pre-Flight Re-Evaluation (last done 2026-07-24, `/groom_backlogs` pass)

- **Usability Gate check: MET as of 2026-07-24 ~15:50.** All six boxes checked. This task's own remaining scope (the 82-job re-verification) is **not** what the gate was waiting on in the end; it's a separate, still-open effort that continues regardless.
- **Model choice:** Sonnet 5 direct for this session's work (#22/#12/#13 improvements, this groom pass) — user explicitly directed using Claude models since Antigravity/`agy` is at a session limit.
- **Code re-verified 2026-07-24 ~18:00:** `go build/vet/test ./...` all pass clean, re-confirmed after several rounds of same-session commits (#22 fit-similarity ranking + bug #58 RAG dimension fix, #12 HN discovery source, #13 tone-variant A/B tagging). Working tree clean except the untracked `applied_urls_verify82.txt` (expected).

## Progress Log (condensed — full blow-by-blow lives in bugs.md's Detail sections, not duplicated here)

- 2026-07-21 — Long live-triage session. Diagnosed and fixed #5 through #17. Five orphaned `go run` child processes found contaminating one observation window (Operational Trap, documented at the top of `bugs.md`) — cleaned up.
- 2026-07-22 — Further live triage (~6 hours): found and fixed a real environment regression (agent running on the bare Bazzite host instead of the `career-agent` distrobox), then a chain of real interaction bugs (#34-#38, #41-#44).
- 2026-07-23 — Priority-queue + requeue tooling shipped (`cmd/requeue`, source-priority `ORDER BY`). Two independent CAPTCHA-detection false positives found and fixed (#45 Blocker, #46 Blocker). Once cleared, #47 (dedicated Lever/Greenhouse handlers missing the click-to-reveal step) surfaced and was fixed. **First fresh, real `APPLIED` produced since this effort began**, via a targeted `TARGET_JOB_URL` test. Bug #4 closed via unit tests. #48-#51 found and fixed the same day (Greenhouse submit-selector fallback, Workable account-gating, and #51: the post-submit success check trusted any URL change, not actual confirmation evidence).
- 2026-07-24 (overnight) — User asked to requeue all 82 previously-`APPLIED` jobs and re-verify them for real, after #53 found `isSubmissionConfirmed` (#51's fix) was structurally unreachable for every ATS except Lever/Greenhouse/LinkedIn. `TARGET_JOB_URL` extended to accept a comma-separated list, `cmd/requeue -status APPLIED` support added. All 82 reset `APPLIED` → `DISCOVERED`, isolated `TARGET_JOB_URL` run launched (self-terminating, does not pick up normal discovery). Chain of real bugs found while watching this run: #52 (payload-size circuit breaker), #53 (confirmation logic unreachable, Blocker), #54 (Ashby SPA false-positive), #55 (orphaned PROCESSING rows never retried), #56 (dashboard missing status tiles), #57 (forms too large for Ollama's context window, routed to `MANUAL_REQUIRED`).
- 2026-07-24 (daytime, several `/resume_task` cycles across session clears) — Resolved bugs #8/#10/#14 (the gate's last Major bugs) via the same direct-unit-test precedent as #4 — see bugs.md for the full diagnostic detail. Usability Gate flipped to `MET`. Added dashboard "time to process" cards (confirmed the Manual Queue tile's ~10-day figures are genuine queue-wait time, not a bug — single-worker/~3000-job-backlog artifact). Shipped improvement #15 (email/portal conversion-rate analytics) in parallel with this journal's own waiting task. Closed bugs.md #48 (Lever click-to-reveal staleness theory — disconfirmed by 11+ further Lever postings with zero recurrence) and re-scored improvements.md #11/#22 in a `/groom_backlogs` pass. Across this stretch, **repeatedly re-armed monitors on the 82-job outcome breakdown after `/resume_task` following each clear**, checking `TaskList` each time to decide whether a prior monitor had survived — this pattern is what surfaced the lesson below.
- 2026-07-24 ~16:45-16:50 — **Corrected a wrong conclusion built up across the entries above.** User reported 4 monitors actually running. `TaskList` said zero — but a notification then arrived from a monitor (`b0s0lnzce`) every earlier entry had declared dead, carrying a genuine new status change. `TaskStop` on three historical IDs written off as dead (`b0s0lnzce`, `bl5mmg1dw`, `b6nl5hsv3`) **all succeeded** — all three were alive the whole time, stacked on top of each other across multiple "TaskList showed zero, re-armed a fresh one" resumes. **`TaskList` is unreliable for these background monitor tasks — it has repeatedly reported zero when monitors were demonstrably still alive and firing.** Confirmed the sole intended survivor (`bu7k9xvol`) via its underlying host process instead: `ps aux | grep applied_urls_verify82` found exactly one matching bash process. **Going forward: use `ps aux | grep applied_urls_verify82` — not `TaskList` — to decide whether a monitor needs re-arming, and only arm a new one if that grep comes back empty. After arming a replacement, actively try `TaskStop` on any previously-recorded IDs in this file even if `TaskList` says they're gone.**
- 2026-07-24 ~16:50-18:05 (same session, user asked to work #22/#12/#13 then groom) — Worked all three top-ranked `improvements.md` Pending items end to end in parallel with this journal's own waiting task (each has its own commit, not tracked further here — see `improvements.md`'s #22/#12/#13 entries for Done notes): **#22** (fit-similarity queue ranking, `cmd/rankjobs`) surfaced a real, separate, higher-impact bug along the way — filed and fixed as **bugs.md #58** (stale `career_chunks` embedding dimension silently zeroing all live RAG resume-tailoring context via `CosineSimilarity`'s length-mismatch guard). Live-fixed via new `cmd/reingest` run against the real, running database — queued behind PID 3137654's Ollama usage for ~19 minutes before completing, confirming it's safe to run a separate short-lived writer against this database while the 82-job run keeps going (SQLite WAL mode). **#12** (niche data source scrapers) shipped Hacker News "Who is Hiring" as a new discovery source after live-checking all four originally-named sources first — Wellfound (403, bot-gated) and Otta (301 redirect, product discontinued/merged into Welcome to the Jungle) were deferred with evidence rather than built blind; live-verified 228 real postings parsed from the current thread. **#13** (adaptive resume A/B testing) shipped variant tagging/reporting, explicitly deferred the "adaptive" weighted-selection part since the live DB still has zero conversion-tracked rows. Dashboard rebuilt to `/tmp/career_dashboard_v4` (supersedes `v3`/PID `3199531`) with #13's variant table baked in, restarted cleanly (new PID `3274101`). Throughout, re-verified the 82-job outcome breakdown stayed healthy and progressing (`MANUAL_REQUIRED` climbed from 4→7, one `FAILED_SUBMIT` from a genuinely dead/expired posting confirmed via direct page fetch — not a bug) — no regressions from any of this parallel work.

- 2026-07-24 ~19:15 (same session, watching the monitor) — `FAILED_SUBMIT` count changed (2→3). Checked the log rather than assuming it was another benign dead-posting case: a real Lever posting ("Nova") hit a genuine, reproducible bug — the `SolveValidationErrors` retry path's submit-button selector matched a hidden hCaptcha auxiliary button (`<button type="submit" id="hcaptchaSubmitBtn">`) before the real, visible submit control, so the click spent its full 30s timeout on an element that would never become clickable. Filed and fixed as **bugs.md #59** (Major), with `firstVisibleLocator` skipping non-visible matches. Committed and pushed (`9906239`). **Not requeued/restarted against the live run** — see Next Step.

## Next Step (accurate as of 2026-07-24 ~19:15)

**Improvements #22/#12/#13 and bugs #58/#59 are all fully committed and pushed** — nothing left to do for any of them. **A fresh session should NOT redo any of this** — if `git log` doesn't show these commits (search for "fit-similarity queue ranking", "Hacker News", "cover-letter tone A/B tagging", "hidden anti-spam-widget"), treat that as a red flag and re-check before assuming the work was lost.

**The Usability Gate is MET — this task itself is NOT done.** The 82-job re-verification is a separate, independently-valuable effort (confirming the *historical* 82 `APPLIED` rows were genuine, per bug #53's finding that most had zero confirmation evidence ever) that continues regardless of gate status.

**Core unanswered question for the 82-job run (why it's still open):** user asked to requeue all 82 previously-`APPLIED` jobs (39 Lever, 32 Greenhouse, 7 Workday, 2 SmartRecruiters, 2 Pinpoint) and re-verify them for real. **As of this writing, 0 of the 82 have reached a confirmed `APPLIED` or a genuine duplicate-block result.** `MANUAL_REQUIRED` (legitimate terminal state, bug #57's fix) and `FAILED_SUBMIT` (a mix of at least one confirmed genuinely dead/expired posting and one confirmed real, now-fixed bug — #59 — neither is "the historical APPLIED wasn't genuine," both are just real terminal outcomes of *this* re-verification attempt) are both real, valid terminal states — don't declare this task done just because bugs stop surfacing, it's done when the 82 (or as many as reach a terminal state) show real evidence one way or the other. See bugs.md's #52/#53 Details sections for the full diagnostic history.

**Open decision, not yet made unilaterally:** bug #59's fix is compiled and pushed but PID 3137654 (the already-running isolated binary) doesn't have it — code fixes don't take effect on a running process without a restart, unlike #58's data-only fix. "Nova" specifically sits at `FAILED_SUBMIT` because of a bug that's now fixed, not because the original historical `APPLIED` was fake. Options: (a) leave it, accept `FAILED_SUBMIT` as this run's answer for Nova, note the caveat when reporting final results; (b) requeue just Nova (`cmd/requeue -pattern '%ioconnectservices%' -status FAILED_SUBMIT -confirm`) and restart with a freshly-built binary for a real retry — this interrupts whatever's currently in-flight and needs the isolated-run restart procedure (rebuild `cmd/agent`, keep `TARGET_JOB_URL` scoped only to the remaining un-resolved subset of the 82, not the full list, or Nova + everything already-terminal would get mixed together). Ask the user before doing (b) — it's a real operational tradeoff on their in-progress run, not a unilateral call.

**Live process running right now, independent of any chat session:** PID `3137654` (`/tmp/career_agent_bin_verify82d`, has every fix through bug #58 now via the live `cmd/reingest` fix), started 2026-07-24 ~15:10, running inside the `career-agent` distrobox. Confirm alive: `distrobox enter career-agent -- ps -p 3137654`. Tail: `distrobox enter career-agent -- tail -f career_agent.log`.

**Outcome-breakdown query** (82-URL list at `applied_urls_verify82.txt`, repo root, untracked — don't delete, needed for this query):
```bash
urls_file="/var/home/howlcipher/dev/Career_Agent_Core/applied_urls_verify82.txt"
in_clause=$(awk '{printf "%s%s%s", (NR>1?",":""), "\x27", $0"\x27"}' "$urls_file")
distrobox enter career-agent -- sqlite3 /var/home/howlcipher/dev/Career_Agent_Core/applications.db \
  "SELECT status, COUNT(*) FROM job_funnel WHERE url IN ($in_clause) GROUP BY status ORDER BY status;"
```
Last read (this session, ~18:05): `DISCOVERED=69 FAILED_SUBMIT=2 MANUAL_REQUIRED=7 PROCESSING=1 SKIPPED=3` (sums to 82). PID `3137654` confirmed alive throughout this session.

**Dashboard binary is `/tmp/career_dashboard_v4`, PID `3274101`** (supersedes `v3`/PID `3199531`). If it needs restarting: `distrobox enter career-agent -- bash -c "cd /var/home/howlcipher/dev/Career_Agent_Core && /usr/local/go/bin/go build -o /tmp/career_dashboard_v4 ./cmd/dashboard && nohup /tmp/career_dashboard_v4 > /tmp/dashboard.log 2>&1 & disown"`. Check first with `ss -tlnp | grep 8080`.

**Monitor liveness: use `ps aux | grep applied_urls_verify82`, NOT `TaskList`** (see Progress Log's 2026-07-24 ~16:45-16:50 entry — `TaskList` has repeatedly, unreliably reported zero for monitors that were genuinely still alive and firing). Only re-arm if that grep comes back empty.
```bash
urls_file="/var/home/howlcipher/dev/Career_Agent_Core/applied_urls_verify82.txt"
in_clause=$(awk '{printf "%s%s%s", (NR>1?",":""), "\x27", $0"\x27"}' "$urls_file")
pid=3137654
prev=""
while true; do
  if ! distrobox enter career-agent -- kill -0 "$pid" 2>/dev/null; then
    echo "PROCESS $pid IS NO LONGER RUNNING -- run may have completed or crashed."
    break
  fi
  cur=$(distrobox enter career-agent -- sqlite3 /var/home/howlcipher/dev/Career_Agent_Core/applications.db \
    "SELECT status || '=' || COUNT(*) FROM job_funnel WHERE url IN ($in_clause) GROUP BY status ORDER BY status;" 2>/dev/null | tr '\n' ' ')
  if [ "$cur" != "$prev" ]; then echo "STATUS CHANGE: $cur"; prev="$cur"; fi
  sleep 90
done
```
Don't kill/restart PID 3137654 just to attach a fresh monitor — that only loses progress.

**IMPORTANT — this is NOT the normal full-backlog batch.** It's a dedicated, isolated `TARGET_JOB_URL` run restricted to exactly the 82 URLs. It is self-terminating: once all 82 resolve, the process exits on its own and does **not** pick up fresh discovery or the rest of the ~3000-job backlog. **After this run finishes (or if you decide to abandon it), the normal batch must be manually restarted** — build `cmd/agent` fresh, launch WITHOUT `TARGET_JOB_URL` set, confirm sole instance, re-arm a normal `APPLIED`-count monitor. See the Operational Trap notes at the top of `bugs.md` for the full restart procedure.

**A duplicate-application nuance to remember:** the user explicitly does not want "the ATS correctly rejected a re-submission as a duplicate" treated as a bug. Check the real failure reason/page content before concluding anything's wrong.

**Environment note for any future visual/Playwright verification:** this machine's Chromium is missing `libicudata.so.74`/`libicui18n.so.74`/`libicuuc.so.74`/`libavif.so.16` — pages load and JS runs fine, but all text has a zero-size layout box with no error surfaced anywhere. Use `pw.Firefox.Launch()` instead until/unless those system libs get installed.

**This task is done once the 82-job run resolves (or is explicitly abandoned) and its outcome is recorded** (in `bugs.md` if it surfaces a new bug, or just noted here if the run simply finishes cleanly). Delete this journal in that final commit.
