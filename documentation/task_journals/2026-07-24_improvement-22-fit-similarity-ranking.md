# Task Journal: improvements.md #22 — Rank discovery queue by resume-fit similarity

## Summary

- **Task:** improvements.md #22 — blend an embedding-similarity signal (job title/company vs. resume) into `GetDiscoveredJobs`'s `ORDER BY`, alongside the existing `sourcePriorityCASE` platform-reachability tiers. User explicitly requested this plus #12 and #13, then a groom pass, in one sitting ("we need to improve this to get more results, we are currently sitting at 0 applied").
- **Status:** In progress
- **Started:** 2026-07-24
- **Agent and model:** Claude Code / Sonnet 5 (orchestrator and implementer — user explicitly directed using Claude models directly since Antigravity/`agy` is at a session limit; local Ollama is also busy serving the live 82-job re-verification run, so heavy delegation there is undesirable too)

## Pre-Flight Re-Evaluation

- **Usability Gate check:** MET (2026-07-24, confirmed in `bugs.md`). Zero open bugs anywhere in `bugs.md`'s Ranked Backlog (all rows Resolved). `improvements.md`'s Pending rows are fair game.
- **Model choice:** Sonnet 5, this session, direct implementation. `agy` unavailable (session limit per user). Local Ollama is the only other delegation option but is actively serving the live 82-job re-verification (PID 3137654) — adding a delegate's workload there risks slowing that run further, and this item's own investigation (see `improvements.md`'s existing #22 detail section) already flagged this exact contention concern before the user confirmed "go ahead."
- **Skills routed:** `software_development` (general Go implementation), `database_management` (new column + migration on `job_funnel`).
- **Code re-verified 2026-07-24:** `pkg/storage/manager.go`'s `GetDiscoveredJobs`/`sourcePriorityCASE` unchanged since the investigation. `pkg/parser/rag.go` has `CosineSimilarity` already; `career_chunks` table has 8 rows live (confirmed via `sqlite3` query) — resume embeddings already exist from `cmd/agent`'s RAG ingestion, no separate ingestion step needed for this item.

## Plan

- [ ] Add `fit_similarity REAL` column to `job_funnel` (schema + idempotent migration, mirroring the existing `migrateJobFunnelLastUpdated` pattern)
- [ ] Add `storage.GetJobsMissingFitSimilarity(limit int)` and `storage.UpdateFitSimilarity(url string, score float32)`
- [ ] Add `parser.BestSimilarity(queryEmbedding []float32, chunks []storage.CareerChunk) float32` (max cosine similarity across resume chunks)
- [ ] Blend into `GetDiscoveredJobs`'s `ORDER BY`: `sourcePriorityCASE` stays primary (platform reachability is a harder constraint than topical fit), `COALESCE(fit_similarity, -1) DESC` secondary, `id` tertiary tie-break — NULL (not-yet-backfilled) rows sort last within their tier, so this is a strict improvement, never a regression, for any row not yet scored
- [ ] New `cmd/rankjobs` CLI to backfill `fit_similarity` for `DISCOVERED` rows missing it — bounded `-limit` (default modest, given Ollama contention with the live run), retry-on-transient-error matching `cmd/agent`'s existing pattern
- [ ] Tests: migration idempotency, `GetJobsMissingFitSimilarity`, `UpdateFitSimilarity`, `BestSimilarity`, and `GetDiscoveredJobs` ordering (same tier, similarity breaks the tie)
- [ ] `go build/vet/test ./...`, update `improvements.md` #22 to Done, commit, push, delete this journal

## Progress Log

- 2026-07-24 (start) — Investigated existing RAG infrastructure (`pkg/parser/rag.go`'s `CosineSimilarity`, `storage.GetAllCareerChunks`/`career_chunks` table, `cmd/agent`'s existing ingestion-on-first-run pattern) and `GetDiscoveredJobs`/`sourcePriorityCASE` in `pkg/storage/manager.go`. Confirmed the item's own investigation notes are still accurate: `fit_score` is dequeue-time only, `career_chunks` already has embedded resume content (8 rows live) so no fresh ingestion is needed, just a query-time embedding per job title/company and a cosine-similarity lookup against the existing chunks.
- 2026-07-24 — Implemented all planned pieces: `fit_similarity REAL` column + `migrateJobFunnelFitSimilarity` (idempotent, mirrors `migrateJobFunnelLastUpdated`), `GetJobsMissingFitSimilarity`/`UpdateFitSimilarity`, `parser.BestSimilarity`, `GetDiscoveredJobs`'s `ORDER BY` blended with `COALESCE(fit_similarity, -1) DESC` as a secondary sort after `sourcePriorityCASE`, new `cmd/rankjobs` CLI (bounded `-limit`, default 200). Tests added and passing: `TestMigrateJobFunnelFitSimilarity`, `TestGetJobsMissingFitSimilarity`, `TestGetDiscoveredJobsOrdersByFitSimilarityWithinTier`, `TestBestSimilarity`/`TestBestSimilarityEmptyChunks`. `go build/vet/test ./...` all green.
- 2026-07-24 — **Found a real, separate, higher-impact bug during live verification**, not just an #22 wrinkle: a 3-job live `cmd/rankjobs -limit 3` run against the real DB returned `fit_similarity = 0.0` for every job. Root-caused: `career_chunks` stores 3072-dim embeddings but the configured `nomic-embed-text` model actually produces 768-dim vectors (confirmed via Ollama's `/api/show`) — `CosineSimilarity`'s length-mismatch guard silently returns 0 for every comparison. This has been silently degrading `cmd/agent`'s live RAG resume-tailoring context (`parser.RetrieveTopK`, called every job) for its entire live history, no error ever surfaced. Filed as **bugs.md #58** (Resolved) and fixed: extracted `parser.IngestResumeChunks`/`CareerChunksNeedReingest`, wired a self-healing dimension check into `cmd/agent`'s startup RAG-ingestion block, added a new `cmd/reingest` CLI. New tests: `TestCareerChunksNeedReingest`, `TestIngestResumeChunks`, `TestIngestResumeChunksSkipsFailedEmbeddings`. `go build/vet/test ./...` still all pass after this fix.
- 2026-07-24 — Reset the 3 poisoned `job_funnel.fit_similarity = 0.0` rows (from the pre-fix test run) back to `NULL` so they get correctly re-scored, not silently kept as false data. Launched `cmd/reingest` against the real, live `applications.db` in the background (task `beiax2rge`) to fix the live data without restarting PID 3137654 (the in-progress 82-job re-verification) — `career_chunks` is read fresh per job via `RetrieveTopK`, so this takes effect on that process's next job once it lands, no restart needed. **As of this entry, still queued behind the live agent's single-Ollama-slot usage** (a direct probe timed out at 90s while PID 3137654 held the slot on a `ProcessJobApplication` call) — not yet confirmed complete. `improvements.md` #22 and `bugs.md` #58 both updated with Done/Resolved notes and full diagnostic detail, but **not yet committed** — holding the commit until the live re-ingestion is confirmed to actually produce non-zero, non-uniform similarity scores, per the Working Protocol's "verify end to end before committing."

## Next Step

Check whether background task `beiax2rge` (`cmd/reingest` against the live DB) has completed. Once it has: (1) confirm `career_chunks` now stores 768-dim embeddings (`python3`/`sqlite3` check, same as the diagnostic query used to find the bug), (2) re-run `cmd/rankjobs -limit 3` against the same 3 real jobs and confirm non-zero, non-uniform `fit_similarity` values land, (3) commit everything (storage/parser/cmd changes, both backlog files, this journal's deletion) as a single commit (or two — #22's feature and #58's bug fix are tightly coupled findings from the same session, judgment call on split vs. combined is fine either way), push, then move to improvement #12 per the user's request to also work #12, #13, then a groom pass.
